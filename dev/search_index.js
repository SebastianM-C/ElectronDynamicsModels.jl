var documenterSearchIndex = {"docs":
[{"category":"section","location":"electron_batches/#Electron-batching:-the-host-memory-model-of-a-field-run","page":"Electron batching","text":"A field run has two very different memory footprints. On the device it is the cube: one set of accumulation buffers, reused by every electron, Nx × Ny × 3 × N_samples per bucket. On the host it used to be the trajectories: the driver solved the whole ensemble up front, kept every electron's splines for the entire field phase, and uploaded them to the kernel one at a time.\n\nEach electron carries a cubic spline of its 8-component state and one of its 4-acceleration, on the same knots: about 3 + 4·8 doubles per knot for the state and 3 + 4·4 for the acceleration, ≈ 320 bytes per knot. At the campaign convention of 16 knots per proper-time carrier period a production trajectory has ≈ 24 200 knots — ≈ 7.4 MB per electron (measured: 17.0 GB of splines for N = 2000 above a 2.2 GB Julia baseline). At N = 16 000 that is ≈ 118 GB of host memory that must stay resident for the whole field phase, and the download then adds one full copy of the cube (12.9 GB for a 401² × 1666 total-mode cube). On a 123 GB host the run reached 121 GB RSS and was killed by the OOM killer at the end of a 44-minute field phase.\n\nNothing in the accumulation needs all the electrons at once.","title":"Electron batching: the host memory model of a field run"},{"category":"section","location":"electron_batches/#The-knob","page":"Electron batching","text":"EDM_ELECTRON_BATCH = B makes scripts/thomson_scattering.jl and scripts/inverse_thomson_scattering.jl solve and accumulate the electrons B at a time: solve a batch, run its launches into device buffers that persist across batches, drop its splines, repeat; the cube is downloaded and permuted once, at the end. EDM_ELECTRON_BATCH = 0 (the default) is the old single pass. The last batch is dropped (and its pages returned to the OS — the GC frees them but glibc keeps them in its arenas, where they still count as RSS) before the download, so the two big consumers never coexist:\n\nduring the batches:  ≈ B × 7.4 MB × (2 with the read-ahead)\nat the download:     ≈ one cube copy + 1/16 of a cube in staging\nhost peak         =  max(the two) + the Julia baseline,   independent of N\n\nBoth terms are approximate from below: Julia's scheduler roots the last task that ran on each thread, and glibc keeps freed chunks in its arenas (malloc_trim, which the loop calls, only returns what the arenas can give back), so a batch's pages come back over the following batch rather than immediately.\n\nEDM_ELECTRON_BATCH_OVERLAP = 0 turns off the read-ahead (the solve of batch k+1 runs during batch k's launches) and drops that factor of 2 at the cost of leaving the CPU solve on the critical path. The manifest records [config] electron_batch, keeps [timing] field as the wall time of the accumulation phase (first launch → finished download) and [timing] kernel as the device-event sum, reports [timing] trajectories as the SUM of the batch solve times, and adds [timing] trajectories_overlapped — the part of it the loop never had to wait for because it ran while the GPU was busy. (The launches are enqueued asynchronously, so a batch small enough to fit whole in the launch queue reports ≈ 0 even though it did overlap; at production batch sizes the enqueue throttles on the queue and the number is meaningful.)","title":"The knob"},{"category":"section","location":"electron_batches/#The-API","page":"Electron batching","text":"Batching is a driver-level feature: the kernels, the per-electron uploads and the launch order are untouched. accumulate_field takes two extra keywords:\n\nacc = accumulate_field(trajs[1:500],    screen, alg, backend; finish = false)              # keep the buffers\nacc = accumulate_field(trajs[501:1000], screen, alg, backend; buffers = acc, finish = false)\nfld = accumulate_field(trajs[1001:1500], screen, alg, backend; buffers = acc)              # download once\n\nfinish = false returns the live FieldAccumulator — the device buffers, their mode, their device and the running electron count — instead of the downloaded cube; passing it back as buffers accumulates into the same buffers. finish_field downloads an accumulator explicitly, without a further batch of electrons. accumulate_field_sharded takes the same pair and keeps one ShardedFieldAccumulator per device: every batch is sharded over the same devices, and the existing device reduce runs once, on the finishing call.","title":"The API"},{"category":"section","location":"electron_batches/#Numerics","page":"Electron batching","text":"On one device a batched run is bit-identical to the single-pass run. The electron loop, the uploads and the launch order do not change, and the launches of one stream are serialized, so the same per-electron contributions are added to the same buffers in the same order.\n\nThe multi-device path is not bit-identical, and was not before: batching re-shards each batch across the devices, so a given device sums a different subset than it would in one pass. Floating- point addition is not associative, so the cube moves in the last bits — the same tolerance the sharded path already carries against the single-device call (relative L2 < 1e-12; measured ~1e-16 in test/gpu_radiation.jl).\n\nThe host-side per-batch products fold exactly: the window-coverage check is per electron, so the batches' records concatenate; the γ(τ) trace reduces as batch sums and elementwise extrema.","title":"Numerics"},{"category":"section","location":"electron_batches/#Measured","page":"Electron batching","text":"The benchmark strong cell scaled down to N = 2000 (γ = 5, 401², 1666 samples, total mode, Newton n_iters = 2, EDM_COEF_REUSE=1, EDM_SAMPLE_CHUNKS=4) on a W7900, RSS sampled every 2 s:\n\n EDM_ELECTRON_BATCH=0 =500\npeak RSS 26.1 GiB 18.4 GiB\nRSS during the field phase 13.4 GiB (all N splines) 6.0–9.7 GiB (≤ 2 batches)\n[timing] field 600.3 s 605.4 s\n[timing] kernel median per launch 288.9 ms 289.3 ms\n[timing] trajectories 43.1 s 47.4 s (31.7 s overlapped)\nwall 731 s 709 s\n\nThe plateau is the part that scales with N: at N = 16 000 the unbatched run holds ~92 GB of splines there, which is what filled a 123 GB host.\n\nThe cell itself, N = 16 000 at EDM_ELECTRON_BATCH=2000 on the same W7900, then runs in 30.9 GiB peak RSS: [timing] field 4682.7 s, [timing] kernel 4642.5 s (16 000 launches, 289.6 ms median), [timing] trajectories 314.9 s of which 271.4 s ran behind the GPU, 0.675 TFLOP/s = 61 % of the card's measured FP64 peak, full window coverage.","title":"Measured"},{"category":"section","location":"experimental/#Experimental","page":"Experimental","text":"The ElectronDynamicsModels.Experimental submodule holds the batched Tsit5 GPU path (GPUKernelTsit5). It is production staging under active development: retained for electron-batching experiments and not yet validated against the reference solver (the committed accuracy test exercises GPUKernelRK4 only). Prefer the promoted GPU API documented on the Home page.","title":"Experimental"},{"category":"type","location":"experimental/#ElectronDynamicsModels.Experimental.BatchedGPUSplines","page":"Experimental","text":"BatchedGPUSplines{D, V, M, VO, VK}\n\nFlat-storage GPU representation of B trajectory splines.  Each electron's data lives at indices offsets[e]+1 : offsets[e]+Ns[e] in the flat arrays. The last row of c1/c2 per electron is unused (interval count = Ns[e]-1) but kept aligned with knot indices for simpler addressing.\n\n\n\n\n\n","title":"ElectronDynamicsModels.Experimental.BatchedGPUSplines"},{"category":"type","location":"experimental/#ElectronDynamicsModels.Experimental.GPUKernelTsit5","page":"Experimental","text":"GPUKernelTsit5\n\nSentinel solver type that selects the batched Tsit5 GPU kernel path.\n\n\n\n\n\n","title":"ElectronDynamicsModels.Experimental.GPUKernelTsit5"},{"category":"method","location":"experimental/#ElectronDynamicsModels.Experimental.to_batched_cpu-Tuple{AbstractVector{<:TrajectoryInterpolant}}","page":"Experimental","text":"to_batched_cpu(trajs_chunk)\n\nPack B TrajectoryInterpolants into a host-side BatchedGPUSplines (still Vector/Matrix-backed).  Adapt.adapt to a backend afterward to upload.\n\n\n\n\n\n","title":"ElectronDynamicsModels.Experimental.to_batched_cpu"},{"category":"method","location":"experimental/#ElectronDynamicsModels.accumulate_potential-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, GPUKernelTsit5, KernelAbstractions.Backend}","page":"Experimental","text":"accumulate_potential(trajs, screen, ::GPUKernelTsit5, backend; batch_size = 8, n_substeps = 1)\n\nBatched GPU path: process electrons in chunks of batch_size, with each kernel launch handling all B electrons in the batch.  Compared to the single-electron GPUKernelRK4 path:\n\none kernel launch per batch instead of per electron (300/B launches at N_macro = 300, B = 8 ⇒ ~38 launches), amortizing launch + sync overhead;\nflat-with-offsets spline storage handles variable length(itp.t) per electron with zero padding waste;\n5th-order Tsit5 with FSAL replaces 4th-order RK4, ~2× better effective accuracy per RHS evaluation.\n\nPer-batch device memory is bounded by B × (spline_size + small), so this scales to arbitrary N_macro.\n\nwarning: Experimental\nThis path is under active development and is not validated against the reference solver.  Use GPUKernelRK4 for trusted results.\n\nArguments\n\nbatch_size: B, number of electrons per kernel launch.  Pick large enough to saturate GPU occupancy (≈ Nx · Ny · B ≳ 4× SM count × 32 cores) but small enough that B × spline_size fits comfortably in VRAM.  8 is a reasonable starting point on a 16 GB card.\nn_substeps: number of fixed-step Tsit5 sub-steps taken between successive saveat slots (and within the bridge step).  Required when ω · δx⁰ lies outside Tsit5's accurate range; FSAL is chained across the equal-dt sub-steps so each adds only ~6 RHS evals.\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_potential"},{"category":"section","location":"gpu_spline/#The-device-spline:-layout,-search-and-the-register-cached-interval","page":"The device spline","text":"The GPU kernels evaluate every electron's worldline through a natural cubic spline of its state, GPUCubicSpline, built once per electron on the host from the trajectory's DataInterpolations.CubicSpline and uploaded with the acceleration spline on the same knots. This page states exactly what the device implementation does, why, and how it was measured, so that benchmark manifests and the upstreaming of the interpolant can refer to one description.","title":"The device spline: layout, search and the register-cached interval"},{"category":"section","location":"gpu_spline/#Storage:-column-major-N-D","page":"The device spline","text":"GPUCubicSpline{D} holds the knot times t (length N), the interval widths h, the second derivatives z and the two per-interval linear coefficients c1, c2, the last three as N × D ((N-1) × D) matrices: for a fixed component d the knots are contiguous. The value at τ on interval i (t[i] ≤ τ < t[i+1]) is the standard natural-spline form\n\nS(τ) = (z[i]·dt2³ + z[i+1]·dt1³) / (6 h[i+1]) + c1[i]·dt1 + c2[i]·dt2,   dt1 = τ − t[i], dt2 = t[i+1] − τ\n\nwith c1[i] = u[i+1]/h[i+1] − z[i+1]·h[i+1]/6 and c2[i] = u[i]/h[i+1] − z[i]·h[i+1]/6 precomputed on the host. Every evaluation therefore reads t[i], t[i+1], h[i+1] and the four D-vectors z[i], z[i+1], c1[i], c2[i]: 3 + 4D scalars, 35 for the 8-component state.\n\nThe layout is deliberate. The retarded proper time of one pixel advances a small fraction of a knot per observer sample (about 0.03 knot in the benchmark cell), and adjacent pixels of a wave sit on the same or the neighbouring knot, so the reuse of a cache line is along the knot index: column-major puts 16 consecutive knots of one component in one 128-byte line, which a wave's lanes and its next few hundred samples keep hitting. The knot-major alternative (D × N, the array-of-structs layout an upstream Vector{SVector{D}} spline has) lets the compiler merge the component loads into 16-byte accesses and issues 26 fewer load instructions per pass, but it turns a cache line over every two knots; on the MI300X it measured 20 % slower for the Newton kernel and 22 % slower for RK4 at the benchmark cell (39 % more L1 accesses and twice the L2 read requests per sample), and no faster on the W7900 or the RTX 5090. It is kept on the branch feat/knot-major-spline for reference.","title":"Storage: column-major N × D"},{"category":"section","location":"gpu_spline/#Interval-search:-warm-started,-provably-the-same-interval","page":"The device spline","text":"_searchsorted_left(t, x) is the cold binary search for the largest i with t[i] ≤ x, clamped to [1, N-1]. _searchsorted_left(t, x, guess) returns the same index by construction: it checks the guessed interval (two reads), otherwise gallops away from it in the direction of x and bisects the bracket it finds. The kernels keep the interval of the previous evaluation of the same pixel as the guess (spline(τ, guess) -> (value, idx)), and the acceleration spline is evaluated on the interval the state spline found (to_gpu refuses an acceleration spline on different knots). Cubes are bit-identical to the cold search; the time removed is the search's dependent loads, four to six per sample: 1.44× (MI300X) and 1.47× (H100) for the Newton kernel at the benchmark cell, 1.1× on the W7900 and the RTX 5090.","title":"Interval search: warm-started, provably the same interval"},{"category":"section","location":"gpu_spline/#The-register-cached-interval-(coef_reuse)","page":"The device spline","text":"The Newton corrections of one sample (and the RK4 stages of one step) evaluate the spline on the same interval almost always. With coef_reuse = Val(true) on accumulate_potential / accumulate_field, _fetch_interval loads the interval's IntervalCoefs once per sample into registers and _eval_poly evaluates the cubic from them; an evaluation whose τ has left the interval (_in_interval) refetches through the warm-started search. The cold path, the warm-started path and the cached path share the one _eval_poly expression, so the GPU cubes are bit-identical across all three (hashed on the W7900, the RTX 5090 and the MI300X for both kernels); on the CPU the two Val specializations may fuse muladds differently, which test/coef_reuse.jl bounds at the last bit.\n\nMeasured at the benchmark strong cell, Newton n=2, ms per launch: MI300X 39.8 → 35.3 (1.13×; load instructions per sample 113 → 55, L2 read requests 569 → 292), W7900 314 → 280 (1.11×), RTX 5090 148 → 144 (1.03×). The cache costs registers: 128 with a few spills on gfx942 (whose budget at the kernels' occupancy is 64 architectural + 64 accumulation registers), 140 on the W7900, 155 on the 5090 (one block per SM instead of two). RK4 holds the interval across four stages and spills 646 registers on the MI300X (85 ms against 50), so the cache is a loss there.\n\nDefault policy in the solver scripts. EDM_COEF_REUSE unset ⇒ on for the Newton kernel on the ROCm backend only, off otherwise: on Hopper the cached kernel needs 160 registers, which halves the resident blocks per SM and costs more than the loads it saves (H100 NVL 54.9 → 59.8 ms at the benchmark cell), and RK4 spills on gfx942. EDM_COEF_REUSE=1|0 forces it either way. The manifest records the value under [config] coef_reuse. The library kwarg defaults to Val(false).","title":"The register-cached interval (coef_reuse)"},{"category":"section","location":"gpu_spline/#What-the-kernels-do-with-it","page":"The device spline","text":"Both kernels walk one pixel's observer samples in order. The Newton kernel (GPUKernelNewton, n_iters corrections from a warm start) solves the light-cone condition per sample: one spline evaluation per correction plus one for the field, on the cached interval. The RK4 kernel (GPUKernelRK4, n_substeps) marches the retarded time: six evaluations per sample with one substep. The kernel choice is an environment knob (EDM_ACCUM_ALG=newton|rk4, EDM_NEWTON_ITERS, EDM_NSUBSTEPS); for the γ = 5 benchmark cell the two kernels' harmonic maps agree to a relative L2 of 5 × 10⁻⁷ at the backscatter line and 2 × 10⁻⁴ at its second harmonic (a uniform scale factor, not a change of structure).","title":"What the kernels do with it"},{"category":"section","location":"#ElectronDynamicsModels","page":"Home","text":"Documentation for ElectronDynamicsModels.\n\n","title":"ElectronDynamicsModels"},{"category":"section","location":"#GPU-diagnostics-(GPUDiagnostics.jl)","page":"Home","text":"Vendor-neutral runtime diagnostics for the KernelAbstractions kernels, shipped as the GPUDiagnostics.jl package (formerly the lib/GPUDiagnostics sub-package) and re-exported by ElectronDynamicsModels: the device API (gpu_device_count, gpu_device!, gpu_name, …), device-event kernel timing (gpu_event, LaunchTimer, launch_times), telemetry (gpu_sample for one snapshot; the out-of-process sampler with_gpu_sampler whose Julia child takes that sample per device per tick — power / utilization / VRAM everywhere, plus NVIDIA GPM hardware counters where the device has them: achieved SM occupancy, FP64 / DRAM-bandwidth utilization — and gpu_telemetry_stats, which reduces the resulting column table into the manifest's [gpu] keys incl. gpm_*), the measured FP64 peak (measure_peak_fp64_flops), the compile-time resource report (compiled_kernels, kernel_resources: registers, spills, LDS and theoretical occupancy of the kernels the process actually compiled) and the static instruction mix (kernel_instruction_mix: the compiled AMD ISA / NVIDIA SASS counted by class — FP64 fma/add/mul/transcendental/other/packed, FP32, integer, scalar, memory, LDS, control, waits — for the whole kernel and for the hot loop of its control-flow graph, natively or cross-compiled for a target that is not present such as gfx942 or sm_90; fp64_issue_floor turns the per-slot FP64 count and the measured FP64 rate into the FP64-pipe time floor per launch; kernel_ir_mix counts the same job's optimized LLVM IR by typed opcode as the pre-backend reference; scripts/instruction_mix.jl prints the table). The CUDA.jl / AMDGPU.jl extensions supply the vendor methods; the CPU backend has host fallbacks.\n\nAMD hardware counters go through rocprofv3 (there is no in-process counter API on AMD, so the profiler wraps the whole solver process): rocprof_command builds that wrapper around a Cmd under timeout -k (a counter set the hardware refuses aborts rocprofv3 and leaves the child hung — ROCPROF_COUNTER_SETS names the sets verified to collect in one pass on gfx942: instruction issue :sq_issue, wave residency :sq_waves, the L1 pipe :l1_pipe, :fp64, :l2, and its docstring the six-counter L2 set that does not), rocprof_counters parses the resulting CSV into the per-dispatch counters of one kernel (RocprofCounters: values, dispatch durations, the VGPR/AGPR/SGPR/LDS/scratch footprint, the device from the agent info), and rocprof_derived / rocprof_summary / rocprof_manifest_section reduce it — medians with spreads across dispatches, per-slot instruction counts (SQ_INSTS_* × wave size / slots), unit-busy fractions (X_BUSY_sum / (cycles × n_cu)), L1/L2 miss rates, wave wait/active fractions and achieved occupancy — with the normalisation of rocprofv3's own derived metrics: the CSV sums GRBM_GUI_ACTIVE over the dies (8 XCDs on the MI300X), so cycles = GRBM_GUI_ACTIVE / n_xcd, and the SQ wave-cycle counters are in quad-cycles. orchestration/profile_cell.sh <set> <outdir> <tag> [EDM_VAR=val …] runs one solver cell under a set and merges the result into the run manifest's [gpu] as rocprof_* keys (scripts/rocprof_merge.jl, which takes the slots per dispatch from [flops].slots_executed and the launch count) — profile the same cell once per set.","title":"GPU diagnostics (GPUDiagnostics.jl)"},{"category":"type","location":"#ElectronDynamicsModels.FieldAccumulator","page":"Home","text":"FieldAccumulator(screen, backend; mode = Val(:split))\n\nThe device-resident accumulation buffers of accumulate_field, held as an object so that they survive one call and several calls can sum into the same buffers.\n\nBy default (buffers = nothing, finish = true) accumulate_field allocates a set per call and downloads it at the end — the single-call API, unchanged. With finish = false it instead returns the FieldAccumulator holding that call's electrons — after draining the call's launches, so the buffers are safe to hand to another task — and passing it back as buffers on the next call continues the accumulation in place; the cube is downloaded once, by the call that runs with finish = true (or by finish_field).\n\nThis is the memory model behind the solver scripts' EDM_ELECTRON_BATCH knob: the host then holds one batch of trajectory splines instead of every electron's for the whole field phase, while the device keeps the same single buffer set as an unbatched run. The kernels, the per-electron uploads and the launch order are untouched, so on one device a batched run adds the same per-electron contributions in the same order as the single call (bit-identical); across devices the shard composition changes with the batching, which moves the sum by the last bits.\n\nFields: E1, B1 (far field in :split, total in :total), E2, B2 (near field; aliases of E1, B1 in :total, where the kernel sums far + near before the write and never touches them), the mode, the device the buffers live on, and the running n_electrons count.\n\nacc = accumulate_field(trajs[1:500], screen, alg, backend; finish = false)\nacc = accumulate_field(trajs[501:1000], screen, alg, backend; buffers = acc, finish = false)\nfld = accumulate_field(trajs[1001:1500], screen, alg, backend; buffers = acc)   # downloads once\n\n\n\n\n\n","title":"ElectronDynamicsModels.FieldAccumulator"},{"category":"type","location":"#ElectronDynamicsModels.FieldEvaluator","page":"Home","text":"FieldEvaluator(laser)\n\nEvaluate electromagnetic fields E(x,y,z,t) and B(x,y,z,t) at arbitrary spacetime points without running an ODE solver. Internally wraps the laser in a ClassicalElectron to produce a compilable system, then uses build_explicit_observed_function to get a fast compiled function for the field observables.\n\nUsage\n\nThe input is [t, x, y, z] (bare time, not c*t). The speed of light c is obtained from the reference frame and applied internally.\n\n@named world = Worldline(:τ, :atomic)\n@named laser = GaussLaser(; wavelength=1.0, a0=1.0, world)\nfe = FieldEvaluator(laser)\nresult = fe([t, x, y, z])  # (E = [...], B = [...])\n\n\n\n\n\n","title":"ElectronDynamicsModels.FieldEvaluator"},{"category":"type","location":"#ElectronDynamicsModels.GPUCubicSpline","page":"Home","text":"GPUCubicSpline{V, M}\n\nA GPU-compatible cubic spline interpolant. Stores precomputed coefficients in flat arrays (works with both Vector and CuArray). The coefficient matrices are column-major over the knots (N × D: for one component the knots are contiguous), because the kernels' reuse of a cache line is along the knot index — consecutive samples of a pixel and the lanes of a wave sit on the same or the neighbouring knot. The knot-major alternative (D × N) issues fewer, wider loads but turns a cache line over every two knots and measured 20 % slower on the MI300X; see the manual page \"The device spline\".\n\nEvaluates the standard natural cubic spline formula:     S(t) = (z[i]dt2³ + z[i+1]dt1³) / (6h[i+1]) + c1[i]dt1 + c2[i]*dt2 where dt1 = t - t[i], dt2 = t[i+1] - t.\n\nFields\n\nt: knot positions (length N)\nh: interval widths, h[i] = t[i] - t[i-1] (length N, h[1] = 0)\nz: second derivatives at knots (N × D matrix, D = number of components)\nc1: precomputed linear coefficients per interval ((N-1) × D matrix)\nc2: precomputed linear coefficients per interval ((N-1) × D matrix)\n\n\n\n\n\n","title":"ElectronDynamicsModels.GPUCubicSpline"},{"category":"method","location":"#ElectronDynamicsModels.GPUCubicSpline-Tuple{Any}","page":"Home","text":"(spline::GPUCubicSpline)(τ)\n(spline::GPUCubicSpline)(τ, guess) -> (value, idx)\n\nEvaluate the spline at time τ, returning an SVector of interpolated values. The two-argument form starts the knot search at interval guess (see _searchsorted_left) and also returns the interval it used, to be fed back as the next call's guess; its value is bit-identical to the one-argument form.\n\n\n\n\n\n","title":"ElectronDynamicsModels.GPUCubicSpline"},{"category":"method","location":"#ElectronDynamicsModels.GPUCubicSpline-Tuple{DataInterpolations.CubicSpline}","page":"Home","text":"GPUCubicSpline(itp::DataInterpolations.CubicSpline)\n\nConstruct a GPUCubicSpline by extracting precomputed coefficients from an existing DataInterpolations.CubicSpline.\n\n\n\n\n\n","title":"ElectronDynamicsModels.GPUCubicSpline"},{"category":"type","location":"#ElectronDynamicsModels.GPUKernelNewton","page":"Home","text":"GPUKernelNewton\n\nSentinel solver type selecting the Newton light-cone GPU kernel path: the retarded proper time at each saveat slot is obtained by solving the light-cone condition x⁰_target − x⁰(τ) − |r_obs − x⃗(τ)| = 0 with n_iters warm-started Newton corrections, instead of marching the retarded-time ODE as GPUKernelRK4 does.  The residual is evaluated in the equivalent screen-relative light-front spelling f = tₖ − ψ(τ) − ρ²/(R + d³) (see the file header), which keeps Float64 rounding at the interaction scale instead of the ε·Z floor of the absolute coordinates.\n\nPer-slot errors are independent across observer-time slots (no accumulation). Prefer this kernel for boosted forward-scattering geometries (γ ≫ 1), where n_iters = 1–2 beats matched-accuracy RK4 by ~4× in cost; prefer GPUKernelRK4 for rest-electron field maps and deep-floor harmonic B-map studies, where its smoother error spectrum preserves near-floor ring structure. Measured campaign numbers in the header of gpu/kernel_newton.jl.\n\n\n\n\n\n","title":"ElectronDynamicsModels.GPUKernelNewton"},{"category":"type","location":"#ElectronDynamicsModels.GPUKernelRK4","page":"Home","text":"GPUKernelRK4\n\nSentinel solver type that selects the unified GPU kernel path.\n\n\n\n\n\n","title":"ElectronDynamicsModels.GPUKernelRK4"},{"category":"type","location":"#ElectronDynamicsModels.IntervalCoefs","page":"Home","text":"IntervalCoefs{D, T}\n\nEverything a cubic-spline evaluation needs on one knot interval: its end knots, inv(6h) and the four D-vectors z[i], z[i+1], c1[i], c2[i] (3 + 4D scalars). _fetch_interval loads it once; _eval_poly evaluates the cubic at any τ from it. The per-slot solvers can hold it in registers across the Newton corrections (or RK4 stages) of a slot, which leave the interval only at a knot crossing, instead of reloading the coefficients for every evaluation (coef_reuse = Val(true) in the accumulate functions: bit-identical values, fewer loads, more live registers).\n\n\n\n\n\n","title":"ElectronDynamicsModels.IntervalCoefs"},{"category":"method","location":"#ElectronDynamicsModels.ObserverScreen-NTuple{4, Any}","page":"Home","text":"ObserverScreen(x_grid, y_grid, z, x⁰_samples; c)\n\nKeyword form requiring the speed of light c in the working units (e.g. getdefault(world.c)).  c has no default on purpose: the x⁰ = c·t axis is meaningless without the unit system, and a wrong default would silently corrupt δt, FFT frequencies, and recommended_n_substeps.\n\n\n\n\n\n","title":"ElectronDynamicsModels.ObserverScreen"},{"category":"type","location":"#ElectronDynamicsModels.ReduceStats","page":"Home","text":"ReduceStats()\n\nWall-clock accounting of the multi-device reduce, filled in by accumulate_field_sharded when passed as reduce_stats: fold_s is the time the finishing tasks spent folding their partials into the accumulator (under the lock; device-to-device adds for reduce = :device, host download-permute-add for :host), n_folds how many partials were folded, and download_s the final download and permute into the host cube (:device only; the :host path downloads as it folds). The solver scripts record them as [timing] reduce_fold / reduce_download, next to field and kernel, so a sharded cell's non-kernel time is visible.\n\n\n\n\n\n","title":"ElectronDynamicsModels.ReduceStats"},{"category":"type","location":"#ElectronDynamicsModels.ShardedFieldAccumulator","page":"Home","text":"ShardedFieldAccumulator(devices)\n\nPer-device FieldAccumulators of a batched accumulate_field_sharded run: one buffer set per device, each holding that device's shard of every batch accumulated so far. Created by the first finish = false call and passed back as buffers; the device reduce runs once, on the call with finish = true. accs[i] is nothing until device i has drawn its first shard.\n\n\n\n\n\n","title":"ElectronDynamicsModels.ShardedFieldAccumulator"},{"category":"method","location":"#ElectronDynamicsModels.GaussLaser-Tuple{}","page":"Home","text":"Gaussian laser pulse electromagnetic field.\n\nRepresents a focused Gaussian beam with a temporal envelope. The beam propagates along the z-direction with waist w₀ at focus.\n\nParameters:\n\nλ: wavelength\na₀: normalized vector potential (a0 kwarg)\nw₀: beam waist (defaults to 75λ)\nn_cycles: number of optical cycles to pulse center (determines t₀)\nτ0: temporal envelope half-width\nt₀: pulse center time (= n_cycles × 2π/ω)\nz₀: focus position along z-axis\n\n\n\n\n\n","title":"ElectronDynamicsModels.GaussLaser"},{"category":"method","location":"#ElectronDynamicsModels.LaguerreGaussLaser-Tuple{}","page":"Home","text":"Laguerre-Gauss laser beam electromagnetic field.\n\nRepresents a focused beam with orbital angular momentum (OAM). The beam is characterized by radial index p and azimuthal index m.\n\nParameters:\n\nλ: wavelength\na₀: normalized vector potential (a0 kwarg)\nϕ₀: initial carrier phase (initial_phase kwarg, defaults to 0.0)\nw₀: beam waist (defaults to 75λ)\nradial_index (p): radial mode number, p ≥ 0\nazimuthal_index (m): azimuthal mode number (orbital angular momentum)\ntemporal_profile: :gaussian (pulsed) or :constant (CW)\ntemporal_width: pulse width for gaussian profile (defaults to 100.0)\nfocus_position: focal position along z-axis (defaults to 0.0)\nk_direction: propagation direction unit vector. Currently restricted to [0, 0, 1] (default, +ẑ) or [0, 0, -1] (−ẑ).\n\nReference: Allen et al., Phys. Rev. A 45, 8185 (1992)\n\n\n\n\n\n","title":"ElectronDynamicsModels.LaguerreGaussLaser"},{"category":"method","location":"#ElectronDynamicsModels.LandauLifshitzRadiation-Tuple{}","page":"Home","text":"Landau-Lifshitz Radiation Reaction\n\nImplements the Landau-Lifshitz formulation of radiation reaction for a charged particle. This avoids the runaway solutions of Abraham-Lorentz by eliminating second derivatives.\n\nReferences:\n\nLandau, L.D. & Lifshitz, E.M. \"The Classical Theory of Fields\" §76\nNiel et. al. 2018, 10.1103/PhysRevE.97.043209, eq. 2 and 3\n\nThe Landau-Lifshitz equation in covariant form: dp^μ/dτ = q F^μν uν + (2τₑ/3)[q ∂ν F^μλ u^ν uλ + q²/m F^μν Fνλ u^λ + q²/mc² (F^νλ u^λ) (F_νγ u^γ) u^μ]\n\n\n\n\n\n","title":"ElectronDynamicsModels.LandauLifshitzRadiation"},{"category":"method","location":"#ElectronDynamicsModels.PlaneWave-Tuple{}","page":"Home","text":"Plane wave electromagnetic field.\n\nFor a plane wave with normalized vector potential a₀ = eA/(mc²), electrons can exhibit figure-8 motion when a₀ ~ 1.\n\nReference: Sarachik & Schappert, Phys. Rev. D 1, 2738 (1970)\n\n\n\n\n\n","title":"ElectronDynamicsModels.PlaneWave"},{"category":"method","location":"#ElectronDynamicsModels.UniformField-Tuple{}","page":"Home","text":"Uniform electromagnetic field component.\n\nIn crossed E and B fields with E⊥B and |E| < |B|c, particles drift with velocity v_drift = E×B/B²\n\nReference: Jackson, \"Classical Electrodynamics\", Section 12.4\n\n\n\n\n\n","title":"ElectronDynamicsModels.UniformField"},{"category":"method","location":"#ElectronDynamicsModels.Worldline-Tuple{Symbol, Symbol}","page":"Home","text":"Worldline(parameter, units; name)\n\nConstruct a worldline parameterization context for relativistic dynamics. The returned MTK system carries the Minkowski metric, physical constants, and the chosen integration parameter (independent variable).\n\nparameter is :τ (proper time) or :t (lab time). Both choices describe the same inertial (lab) frame; they differ only in which scalar parameterizes the worldline that the integrator steps along. They are not different Lorentz frames — the components of x^μ, u^μ, F^{μν} are always resolved in the frame where the external fields are defined.\n\nunits is :SI, :atomic, or :natural.\n\n\n\n\n\n","title":"ElectronDynamicsModels.Worldline"},{"category":"method","location":"#ElectronDynamicsModels._device_add!-Union{Tuple{T}, Tuple{KernelAbstractions.Backend, Integer, AbstractArray{T}, AbstractArray{T}}} where T","page":"Home","text":"_device_add!(backend, dev, dst, src) -> dst\n\nFold src, resident on the calling task's current device, into dst, resident on device dev (1-based vendor id), without a host copy of either. Same device (or the CPU backend): a chunked in-place broadcast. Different devices: each 1/16 chunk is copied device-to-device into a staging buffer on dev and added there; the vendor runtime routes the copy over the peer link when the two GPUs can address each other and through host memory otherwise. Should the direct copy be refused by the runtime, the chunk goes through a host slab instead. The calling task's device is restored on return.\n\n\n\n\n\n","title":"ElectronDynamicsModels._device_add!"},{"category":"method","location":"#ElectronDynamicsModels._download_permuted-Union{Tuple{AbstractArray{T, 4}}, Tuple{T}} where T","page":"Home","text":"_download_permuted(buf; backend = nothing, dev = 0, workers = 1) -> Array{T,4}\n\nDownload an accumulation buffer laid out [ix, iy, μ, k] (pixel-fastest for coalesced device writes; observer slot k slowest) into the cube layout [k, μ, ix, iy] without a full-size intermediate. The old permutedims(Array(buf), (4, 3, 1, 2)) held cube + copy on the host — a 2× peak that capped carrier-resolved cube designs at ~half the node's RAM (the permute lives on the host because the >2³¹-element GPU permutedims overflows its 32-bit linear indices, cudaError 700). Downloading contiguous k-chunks through the linear copyto! DMA path and permutedims!-ing each into a view of the preallocated cube keeps the host peak at ~(1 + 1/16)× cube; device memory and kernels are untouched, and every transfer stays far below 2³¹ elements. Works unchanged on the CPU backend (buf::Array).\n\nworkers > 1 spreads the 16 chunks over that many tasks, each with its own staging slab (host peak ~(1 + workers/16)× cube): the download DMA and the single-threaded permutedims! of the chunks then overlap. The tasks pin themselves to dev through gpu_device!(backend, dev), so backend and dev are required for that path.\n\n\n\n\n\n","title":"ElectronDynamicsModels._download_permuted"},{"category":"method","location":"#ElectronDynamicsModels._download_permuted_add!-Union{Tuple{T}, Tuple{Array{T, 4}, AbstractArray{T, 4}}} where T","page":"Home","text":"_download_permuted_add!(out, buf) -> out\n\nIn-place sibling of _download_permuted: download buf ([ix, iy, μ, k]) chunk by chunk and ADD it into the preallocated host cube out ([k, μ, ix, iy]). Two staging buffers of 1/16 cube (the linear download slab and its permuted copy) are the only transient, so folding a device partial into a running host sum costs ~(1/8)× cube of extra host memory — the primitive behind the streamed multi-device reduce, which keeps ONE host cube resident instead of one per device.\n\n\n\n\n\n","title":"ElectronDynamicsModels._download_permuted_add!"},{"category":"method","location":"#ElectronDynamicsModels._find_world-Tuple{ModelingToolkitBase.AbstractSystem}","page":"Home","text":"_find_world(sys)\n\nFind the Worldline subsystem within an external field by checking for the metric tensor gμν and electron mass m_e parameters.\n\n\n\n\n\n","title":"ElectronDynamicsModels._find_world"},{"category":"method","location":"#ElectronDynamicsModels._kz_sign-Tuple{Any}","page":"Home","text":"_kz_sign(k_direction) -> ±1\n\nValidate that k_direction is along ±ẑ and return its sign. Currently only ±ẑ propagation is supported for LaguerreGaussLaser; oblique directions require defining a transverse basis convention that has not yet been implemented.\n\n\n\n\n\n","title":"ElectronDynamicsModels._kz_sign"},{"category":"method","location":"#ElectronDynamicsModels._searchsorted_left-Tuple{Any, Any, Integer}","page":"Home","text":"_searchsorted_left(t, x, guess)\n\nWarm-started variant: returns exactly what _searchsorted_left(t, x) returns (the largest i ∈ [1, length(t)-1] with t[i] ≤ x, or 1), but starts from guess. If x lies in [t[guess], t[guess+1]) the answer is guess after two knot reads; otherwise the search gallops from guess towards x (steps 1, 2, 4, …) and finishes with a binary search inside the bracket it found — about 2·log₂(distance) reads instead of log₂(N). The per-pixel kernels feed the interval of their previous evaluation back in: consecutive slots move the retarded time by a fraction of a knot, so the fast path is the common case. The result is identical by construction (same predicate, same bracket invariants), so kernel output does not change.\n\n\n\n\n\n","title":"ElectronDynamicsModels._searchsorted_left"},{"category":"method","location":"#ElectronDynamicsModels._searchsorted_left-Tuple{Any, Any}","page":"Home","text":"_searchsorted_left(t, x)\n\nBinary search for the interval index: find largest i such that t[i] ≤ x. Clamps to [1, length(t)-1] for evaluation safety. GPU-compatible: no allocations, no dynamic dispatch.\n\n\n\n\n\n","title":"ElectronDynamicsModels._searchsorted_left"},{"category":"method","location":"#ElectronDynamicsModels.a0_from_pulse_energy-NTuple{4, Any}","page":"Home","text":"a0_from_pulse_energy(W, w₀, τ₀, ω; world, mode = (p = 0, m = 2)) -> a₀\n\nCompute the dimensionless vector potential a₀ for a Laguerre-Gauss pulse of total energy W, host-beam waist w₀, field-envelope half-width τ₀, and angular frequency ω. All inputs in the unit system of world (atomic, SI, or natural). mode = (p, m) selects the LG mode; the formula is p-independent (in EDM's normalization), so only |m| matters in practice.\n\nFormula:     a₀² = 2W·|qe|² / (ε₀ c³ A(p,m) (me ω)² w₀² τ₀ √(π/2)) with A(p,m) = (π/2)·(|m|!)².\n\nUse this script-side when u0_constructor (e.g. SVector{8}) precludes LaguerreGaussLaser's built-in pulse_energy initialization, since the init sub-problem inherits the constructor with the wrong dimension. Pass the result to LaguerreGaussLaser(; a0 = …) instead.\n\nSee references/lg_pulse_energy_a0.tex for the derivation.\n\n\n\n\n\n","title":"ElectronDynamicsModels.a0_from_pulse_energy"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_field-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, Any}","page":"Home","text":"accumulate_field(trajs, screen, alg; mode = Val(:split), solve_kwargs...)\n\nCompute the radiated electromagnetic field on screen from electron trajs.\n\nFor each electron and pixel, solves the retarded-time ODE (as in accumulate_potential), builds the Liénard–Wiechert Faraday tensor at each observer-time sample via lienard_wiechert_F_split, and coherently sums the fields over electrons.  The far (1/R) and near (1/R²) pieces are accumulated in separate buffers: this avoids the (c² − X𝔞) cancellation at small a₀ and keeps the (near-field-dominated) total from swamping the far-field sum.  The Faraday tensor is antisymmetric and the fields are linear in it, so each contribution is stored in the compact (E, B) basis rather than the redundant 16-component matrix (Σᵢ extract_EB(Fᵢ) = extract_EB(Σᵢ Fᵢ)).\n\nReturns (; E, B, E_far, B_far), each Array{Float64,4} of shape (N_samples, 3, Nx, Ny): E, B are the total time-domain fields (far + near, including their cross term) for screen_observables; E_far, B_far are the far field alone, whose observables are the radiated energy/angular momentum exactly (the near field is recoverable as E − E_far).\n\nmode = Val(:total) returns only (; E, B) (the total), a type-stable trim for callers that don't need the split; Val(:split) (the default) keeps all four.\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_field"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_field-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, GPUKernelNewton, KernelAbstractions.Backend}","page":"Home","text":"accumulate_field(trajs, screen, ::GPUKernelNewton, backend; n_iters = 2, mode = Val(:split), sync_per_electron = true)\n\nField counterpart of the GPUKernelNewton accumulate_potential method: per-slot Newton light-cone solve instead of the RK4 retarded-time march, otherwise identical in buffers, mode, and streaming to the GPUKernelRK4 accumulate_field method. timer = LaunchTimer() records a device-event pair per launch (see LaunchTimer). coef_reuse = Val(true) holds each slot's spline-interval coefficients in registers across the per-slot evaluations (bit-identical; trades ~4D live registers for the coefficient loads, see IntervalCoefs).\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_field"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_field-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, GPUKernelRK4, KernelAbstractions.Backend}","page":"Home","text":"accumulate_field(trajs, screen, ::GPUKernelRK4, backend; n_substeps = 1, mode = Val(:split), sync_per_electron = true)\n\nGPU counterpart of the CPU accumulate_field: per-electron kernel launch performs the retarded-time RK4 integration and Liénard–Wiechert (E, B) accumulation in one pass, coherently summing over electrons on the device.\n\nMirrors accumulate_potential(trajs, screen, ::GPUKernelRK4, backend), but uploads the acceleration spline (to_gpu(traj; with_acceleration = true)) since the far field needs 𝔞μ, and accumulates the split (far/near) Liénard–Wiechert field instead of the 4-potential. Returns (; E, B, E_far, B_far), each (N_samples, 3, Nx, Ny) — identical shape to the CPU accumulate_field: E, B total, E_far, B_far the far field alone (see lienard_wiechert_F_split). mode = Val(:total) returns only (; E, B) (a type-stable trim); Val(:split) (the default) keeps all four.\n\nn_substeps and sync_per_electron behave exactly as in the potential kernel; see accumulate_potential and recommended_n_substeps.\n\nbuffers / finish drive electron BATCHING: finish = false returns the live FieldAccumulator instead of the downloaded cube, and passing it back as buffers accumulates the next batch of electrons into the same device buffers, so the host only ever holds one batch of trajectory splines. The download happens once, on the call with finish = true (or through finish_field); on one device the batched sum is bit-identical to the single call.\n\ntimer = LaunchTimer() records a device-event pair per launch (see LaunchTimer). coef_reuse = Val(true) holds each slot's spline-interval coefficients in registers across the per-slot evaluations (bit-identical; trades ~4D live registers for the coefficient loads, see IntervalCoefs).\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_field"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_field_sharded-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, Any, KernelAbstractions.Backend}","page":"Home","text":"accumulate_field_sharded(trajs, screen, alg, backend;\n                         devices = 1:gpu_device_count(backend),\n                         reduce = :device, reduce_workers = min(4, Threads.nthreads()),\n                         kwargs...)\n    -> (; E, B[, E_far, B_far])\n\nShard trajs across devices (vendor-native 1-based ids) and run the single-device accumulate_field on each shard CONCURRENTLY — one Threads.@spawn task per device, each pinned with gpu_device!(backend, d) so its buffers + kernels land on that GPU — then sum the per-device partials into ONE host cube set. The sum is exact by linearity; only its summation order differs from the single-device call (last-bit).\n\nreduce = :device (default) sums on the GPUs: the first device to finish keeps its buffers as the accumulator, the others fold theirs into it over the device-to-device path as they finish, and one download + permute produces the host cube (the permute runs on reduce_workers threads, each holding a 1/16-cube staging slab). reduce = :host is the streamed host reduce: every partial is downloaded, permuted and added on the host under a lock (one cube resident, one single-threaded full-cube permute per device). reduce_stats = ReduceStats() receives the wall-clock time of the folds and of the final download (see ReduceStats).\n\nbuffers / finish extend the electron BATCHING of accumulate_field to the sharded path: finish = false shards this batch, accumulates it into each device's own buffers and returns the ShardedFieldAccumulator holding them, which the next batch takes as buffers; the reduce and the download run once, on the call with finish = true. Each batch is split over the same devices, so a device that draws nothing from the last batch still folds in the electrons it accumulated earlier. Batching changes which electrons meet on which device, so the summation order — and with it the last bits of the cube — differs from an unbatched run.\n\nNeeds ≥length(devices) Julia threads (julia -t): each per-device task is GPU-bound and blocks its thread on the final device→host copy, so they only overlap on separate OS threads. Each device holds a full prod-size buffer set (see the VRAM budget), so this trades device count for memory, not memory for device count. The same device id may appear more than once (e.g. devices = [1, 1]): the shards then time-share that GPU — pointless for throughput but the exactness check the CPU-backend test relies on.\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_field_sharded"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_potential-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, Any, KernelAbstractions.Backend}","page":"Home","text":"accumulate_potential(trajs, screen, alg, backend::Backend; solve_kwargs...)\n\nCompute the Liénard-Wiechert 4-potential using CPU retarded-time solve and GPU-accelerated accumulation via AcceleratedKernels.\n\nbackend is a KernelAbstractions backend (e.g., CUDA.CUDABackend()). Uses the original trajs (CubicSpline-based) for the CPU retarded-time solve, and converts to GPUCubicSpline internally for the GPU accumulation phase.\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_potential"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_potential-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, Any}","page":"Home","text":"accumulate_potential(trajs, screen, alg; solve_kwargs...)\naccumulate_potential(trajs, screen, alg, ensemblealg; solve_kwargs...)\n\nCompute the Liénard-Wiechert 4-potential on screen from electron trajs.\n\nFor each electron trajectory, solves the retarded-time ODE to map observer time to proper time, then evaluates Aμ = K uμ / (xr · u) at uniform observer-time samples. Returns A[k, μ, ix, iy] — the time-domain 4-potential ready for FFT.\n\nThe two-argument alg form uses a reinit!-based integrator pool for efficient CPU threading. The four-argument form with ensemblealg uses EnsembleProblem for compatibility with GPU backends (e.g., EnsembleGPUKernel).\n\nArguments\n\ntrajs: vector of TrajectoryInterpolant from trajectory_interpolants\nscreen: ObserverScreen defining pixel grid and observer-time samples\nalg: ODE solver algorithm for the retarded-time problem (e.g., Tsit5())\nensemblealg: (optional) ensemble algorithm (e.g., EnsembleGPUKernel(backend))\nsolve_kwargs...: additional keyword arguments passed to the ODE solver\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_potential"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_potential-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, GPUKernelNewton, KernelAbstractions.Backend}","page":"Home","text":"accumulate_potential(trajs, screen, ::GPUKernelNewton, backend; n_iters = 2, sync_per_electron = true)\n\nNewton light-cone GPU path: like the GPUKernelRK4 unified kernel, but the retarded proper time at each saveat slot is found by solving the light-cone condition directly (n_iters warm-started Newton corrections per slot) instead of integrating the retarded-time ODE between slots.  The condition is evaluated in the screen-relative light-front spelling f = tₖ − ψ(τ) − ρ²/(R + d³) (algebraically identical to x⁰_target − x⁰(τ) − |r_obs − x⃗(τ)| = 0; see the file header), with the sample grid carried as offsets tₖ = x⁰_k − z_screen.\n\nSpline-eval budget per slot is n_iters + 1 (the final residual eval doubles as the accumulation eval), vs 4·n_substeps + 1 for the RK4 march; and the per-slot error does not accumulate along the march, so accuracy is set by the convergence of the last Newton step alone.\n\nsync_per_electron as in the GPUKernelRK4 method.\n\nbuffers / finish drive electron BATCHING: finish = false returns the live FieldAccumulator instead of the downloaded cube, and passing it back as buffers accumulates the next batch of electrons into the same device buffers, so the host only ever holds one batch of trajectory splines. The download happens once, on the call with finish = true (or through finish_field); on one device the batched sum is bit-identical to the single call.\n\ntimer = LaunchTimer() records a device-event pair per launch (see LaunchTimer). coef_reuse = Val(true) holds each slot's spline-interval coefficients in registers across the per-slot evaluations (bit-identical; trades ~4D live registers for the coefficient loads, see IntervalCoefs).\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_potential"},{"category":"method","location":"#ElectronDynamicsModels.accumulate_potential-Tuple{Vector{<:TrajectoryInterpolant}, ObserverScreen, GPUKernelRK4, KernelAbstractions.Backend}","page":"Home","text":"accumulate_potential(trajs, screen, ::GPUKernelRK4, backend; n_substeps = 1)\n\nUnified GPU path: per-electron kernel launch performs retarded-time RK4 integration and Liénard-Wiechert accumulation in one pass.  Compared to the two-phase AcceleratedKernels path:\n\nno τ_all intermediate buffer (saves ~N_samples × Nx × Ny × 8 bytes)\nno CPU retarded-time solve, so Phase-1 wall time scales with GPU rather than CPU thread count\naccumulation order eliminates the round(Int, …) saveat-slot mapping that the CPU+AK path needs (slot index k is the loop variable).\n\nn_substeps controls how many fixed-step RK4 sub-steps are taken between successive saveat slots (and within the bridge step).  Required when ω · δx⁰ lies outside RK4's accurate range.  The per-step amplitude error of fixed-step RK4 on a mode of frequency ω is |R(iθ)| − 1 ≈ θ⁶/144 with θ = ω · dt (R the RK4 stability polynomial): at θ = π/2 that is ~7.5%/step, at θ ≈ 0.23 it is ~1e-6.  So to hold per-step error ≤ ε, take n_substeps = ⌈ ω·δx⁰ / (144 ε)^(1/6) ⌉.  For the relativistic Thomson script (a₀ = 10, δt = T/4 ⇒ ω·δx⁰ ≈ π/2), the relevant ω is the highest harmonic present (≈ a₀³ × ωfundamental for nonlinear Thomson), so `nsubsteps ≈ 8` recovers parity with the adaptive-Tsit5 reference.\n\nsync_per_electron (default true) inserts a KernelAbstractions.synchronize before freeing each trajectory's device buffers — safe but serializes the electron loop.  Setting it false drops the per-electron sync and relies on stream-ordered async free (the kernel that still reads the buffers is queued ahead of the free on the same stream), letting electron N+1's upload overlap kernel N.  Verified correct on CUDA and ROCm backends.\n\ntimer = LaunchTimer() records a device-event pair per launch (see LaunchTimer). coef_reuse = Val(true) holds each slot's spline-interval coefficients in registers across the per-slot evaluations (bit-identical; trades ~4D live registers for the coefficient loads, see IntervalCoefs).\n\n\n\n\n\n","title":"ElectronDynamicsModels.accumulate_potential"},{"category":"method","location":"#ElectronDynamicsModels.angular_momentum_flux_z-Tuple{Any, Any}","page":"Home","text":"angular_momentum_flux_z(T, r) -> Real\n\nz-component of the radiated angular-momentum flux density crossing the screen, at screen point r = (x, y, z), derived from the stress-energy tensor T^{μν}. Summed over the screen (× dA) and observer-time samples (× dt) it gives the total radiated L_z; divided by the radiated energy it gives the OAM per photon.\n\nUses the exact Maxwell-stress form x Tᶻʸ − y Tᶻˣ (the z-row of T), the covariant angular-momentum flux M^{z x y} = xᵘ Tᶻᵛ − xᵛ Tᶻᵘ; no far-field approximation. With slots 1=time, 2,3,4 = x,y,z: Tᶻʸ = T[4,3], Tᶻˣ = T[4,2].\n\n\n\n\n\n","title":"ElectronDynamicsModels.angular_momentum_flux_z"},{"category":"method","location":"#ElectronDynamicsModels.canonical_state_order-Tuple{TrajectoryInterpolant}","page":"Home","text":"canonical_state_order(traj::TrajectoryInterpolant) -> Bool\n\nWhether the state spline stores x⁰…x³ at components 1:4 and u⁰…u³ at 5:8 — the layout the GPU kernels assume (they index the state with literal constants). The sol constructor always produces it; a hand-built spline must be arranged that way before to_gpu.\n\n\n\n\n\n","title":"ElectronDynamicsModels.canonical_state_order"},{"category":"method","location":"#ElectronDynamicsModels.extract_EB-Tuple{Any, Any}","page":"Home","text":"extract_EB(F, c) -> (E, B)\n\nRecover the electric and magnetic 3-vectors from an upper-index Faraday tensor F^{μν} — the inverse of faraday.  With (+,−,−,−) indexing (slot 1 is time): Eⁱ = c·Fⁱ⁰ and B = (F⁴³, F²⁴, F³²).  Linear in F, hence commutes with the electron sum.\n\n\n\n\n\n","title":"ElectronDynamicsModels.extract_EB"},{"category":"method","location":"#ElectronDynamicsModels.faraday-Tuple{Any, Any, Any}","page":"Home","text":"faraday(E, B, c) -> SMatrix{4,4}\n\nUpper-index Faraday tensor F^{μν} from the field 3-vectors, in the package's (+,−,−,−) convention: F^{0i} = −Eⁱ/c, F^{ij} = −ε^{ijk}Bᵏ.  Returns a StaticArrays matrix so the same assembly serves both the symbolic models (with Num entries) and the numeric screen reduction.  Its inverse is extract_EB.\n\n\n\n\n\n","title":"ElectronDynamicsModels.faraday"},{"category":"method","location":"#ElectronDynamicsModels.finish_field-Tuple{FieldAccumulator}","page":"Home","text":"finish_field(acc::FieldAccumulator; sink = nothing, workers = 1) -> (; E, B[, E_far, B_far])\n\nDownload the accumulated cube from a FieldAccumulator without adding more electrons — the explicit form of accumulate_field(no_more_electrons, …; buffers = acc). workers > 1 spreads the download's permute over that many tasks (see _download_permuted). sink receives the device buffers instead, exactly as in accumulate_field.\n\nThe accumulator keeps its buffers (and its sum) afterwards; call it again, or keep accumulating into it, if that is what the driver wants.\n\n\n\n\n\n","title":"ElectronDynamicsModels.finish_field"},{"category":"method","location":"#ElectronDynamicsModels.flop_profile-Tuple{GPUKernelNewton}","page":"Home","text":"flop_profile(alg::GPUKernelNewton; mode = Val(:split), n_iters = 2) -> NamedTuple\nflop_profile(alg::GPUKernelRK4;    mode = Val(:split), n_substeps = 1) -> NamedTuple\n\nAlgorithmic FLOP cost of the accumulate_field kernel for alg with the given accuracy knob and mode (Val(:split) / Val(:total), or the symbol), measured on the host by running the kernel body on CountedFloats.Counted{Float64} (see the file header). Returns\n\nflop_per_slot — FLOPs per (electron, pixel, observer sample), and per_slot, its per-category breakdown (add, mul, div, sqrt, fma, pow, trans exact; cmp, other are not FLOPs and are rounded means);\nflop_per_pixel_launch / per_pixel_launch — the per-(electron, pixel) setup cost (window edges, RK4 bridge; for RK4 minus the one inter-slot advance the last slot skips);\nbytes_per_slot — device-buffer read-modify-write traffic per slot (12 or 6 doubles), arithmetic_intensity = flop_per_slot / bytes_per_slot;\nconvention, alg, mode, n (the accuracy knob), n_name.\n\nA run's total is flop_per_slot · slots_executed + flop_per_pixel_launch · N·Nx·Ny (slots_executed from window_coverage; N·N_samples·Nx·Ny when the window is fully covered). Costs milliseconds; the GPU is never touched.\n\n\n\n\n\n","title":"ElectronDynamicsModels.flop_profile"},{"category":"method","location":"#ElectronDynamicsModels.harmonic_bins-Tuple{Integer, Real, Real, Any}","page":"Home","text":"harmonic_bins(N_samples, δt, ω, harmonics) -> Vector{Int}\n\nThe rfft bin indices closest to the harmonics n·ω of the fundamental, for a time series of N_samples samples spaced δt apart. harmonics is any iterable of integers (e.g. (1, 2)). Deduplicates the locator copied across the solver/plot scripts.\n\nErrors when a requested harmonic lies above the sampling Nyquist 1/(2δt): nearest-match would otherwise silently clamp it onto the last rfft bin, and the caller would publish maps labeled with the requested n while holding that bin's content (the inverse-Thomson ≈4γ²ω aliasing trap).\n\n\n\n\n\n","title":"ElectronDynamicsModels.harmonic_bins"},{"category":"method","location":"#ElectronDynamicsModels.harmonic_colorrange-Tuple{Any}","page":"Home","text":"harmonic_colorrange(data) -> (lo, hi)\n\nDefault per-panel color range for harmonic maps: the data extrema, guarded against a degenerate/underflowing panel (falls back to (-1, 1)). See also [symmetric_colorrange].\n\n\n\n\n\n","title":"ElectronDynamicsModels.harmonic_colorrange"},{"category":"method","location":"#ElectronDynamicsModels.harmonic_maps-Tuple{AbstractArray{<:Number, 4}, AbstractVector{<:Integer}}","page":"Home","text":"harmonic_maps(field::NamedTuple, bins) -> Array{ComplexF64,4}   # primary: (;E,B) → 6 components\nharmonic_maps(cube::AbstractArray{<:Number,4}, bins) -> Array{ComplexF64,4}   # generic cube\n\nSpatial maps of a field/potential at the harmonic bins (see harmonic_bins): rfft along the time (first) axis and slice out the rows in bins.\n\nThe field method takes (; E, B) (each (N_samples, 3, Nx, Ny)) and returns (length(bins), 6, Nx, Ny) — components Eˣ Eʸ Eᶻ Bˣ Bʸ Bᶻ (E in 1:3, B in 4:6). This is the primary entry point for the field runs.\n\nThe cube method is the generic core: any (N_samples, n_components, Nx, Ny) array → (length(bins), n_components, Nx, Ny); it also serves the 4-component 4-potential A. Both deduplicate the per-component rfft reduction copied across 5 scripts — the transform is done one component at a time (these cubes are tens of GB at full resolution).\n\n\n\n\n\n","title":"ElectronDynamicsModels.harmonic_maps"},{"category":"function","location":"#ElectronDynamicsModels.iso_mesh","page":"Home","text":"iso_mesh(vol, Xs, Ys, Zs, level) -> GeometryBasics.Mesh | nothing\n\nMarching-cubes isosurface of vol on the grids (Xs, Ys, Zs) (dim 1 ↔ Xs), as a GeometryBasics.Mesh ready for mesh!, or nothing when the level cuts no surface. This is the RPR-compatible replacement for contour!/volume! (unsupported / GPU-segfaulting on RPR backends), and renders identically on GLMakie.\n\n\n\n\n\n","title":"ElectronDynamicsModels.iso_mesh"},{"category":"method","location":"#ElectronDynamicsModels.lienard_wiechert_F-NTuple{5, Any}","page":"Home","text":"lienard_wiechert_F(X, u, 𝔞, K, c) -> SMatrix{4,4}\n\nTotal Liénard–Wiechert field-strength (Faraday) tensor F^{μν} = F_near + F_far, the sum of the two pieces from lienard_wiechert_F_split:\n\nF^{μν} = K [ (Xᵘ𝔞ᵛ − Xᵛ𝔞ᵘ) / (Xu)²  +  (c² − X𝔞)(Xᵘuᵛ − Xᵛuᵘ) / (Xu)³ ].\n\nIndices are upper, matching faraday and the Lorentz force in ChargedParticle. Use lienard_wiechert_F_split when accumulating, to keep the far and near fields in separate coherent sums (see accumulate_field).\n\n\n\n\n\n","title":"ElectronDynamicsModels.lienard_wiechert_F"},{"category":"method","location":"#ElectronDynamicsModels.lienard_wiechert_F_split-NTuple{5, Any}","page":"Home","text":"lienard_wiechert_F_split(X, u, 𝔞, K, c) -> (F_near, F_far)\n\nLiénard–Wiechert field-strength (Faraday) tensor split by power of R into its near-field (1/R²) and far-field (1/R) pieces — the form used for numerically clean, separately-coherent accumulation in accumulate_field.  With Xu = m_dot(X,u) ∝ R and X𝔞 = m_dot(X,𝔞) ∝ R:\n\nF_near = K c² (Xᵘuᵛ − Xᵛuᵘ) / (Xu)³                            ∝ 1/R²\nF_far = K [ (Xᵘ𝔞ᵛ − Xᵛ𝔞ᵘ) / (Xu)²\n          − X𝔞 (Xᵘuᵛ − Xᵛuᵘ) / (Xu)³ ]                        ∝ 1/R\n\nX is the retarded displacement 4-vector Xᵘ = xᵘ_obs − xᵘ_src(τᵣ) (= (R, R·n̂)), u the 4-velocity and 𝔞 the 4-acceleration at τᵣ; K = q/(4πε₀c).\n\nThe subtlety: the −X𝔞 term rides the velocity bivector (Xᵘuᵛ − Xᵛuᵘ) yet scales as 1/R (because X𝔞 ∝ R), so it belongs to the far field. lienard_wiechert_F groups it with the c² term as (c² − X𝔞), which loses ≈log₁₀(c²/X𝔞) significant digits when X𝔞 ≪ c² (small a₀); computing F_far directly never forms that difference.  F_near + F_far is identically lienard_wiechert_F in exact arithmetic.\n\n\n\n\n\n","title":"ElectronDynamicsModels.lienard_wiechert_F_split"},{"category":"method","location":"#ElectronDynamicsModels.observer_window_start-NTuple{4, Any}","page":"Home","text":"observer_window_start(τi, Z, half_width, Rmax; c)\n\nObserver time x⁰ at which the LAST retarded image of proper time τi reaches a square screen of half-width half_width at distance Z from a source disc of radius Rmax centred on the axis, for electrons at rest at τi: the corner pixel (√2·halfwidth from the axis) and the electron on the far rim, `c·τi + hypot(Z, √2·halfwidth + Rmax). Opening the observer window here makes its first sample already see every electron at every pixel, so no sample is partially populated. (The edge-midpoint distancehypot(Z, half_width + Rmax)` is NOT a bound: the pixels outside the screen's inscribed circle then miss the far-side electrons' first samples.)\n\n\n\n\n\n","title":"ElectronDynamicsModels.observer_window_start"},{"category":"method","location":"#ElectronDynamicsModels.phase_winding_fit-Tuple{AbstractVector, AbstractVector}","page":"Home","text":"phase_winding_fit(az, phase; weights = nothing) -> (; slope, intercept, unwrapped)\n\nLeast-squares fit of the azimuthal phase winding: model phase ≈ slope·az + intercept, with az a vector of azimuths sorted ascending (as from ring_pixels) and phase the wrapped phase angle(F) at those points.\n\nBecause angle wraps to (-π, π], a winding field reads as a sawtooth, so a naive fit on the raw series returns slope ≈ 0 regardless of the true winding. The phase is therefore first unwrapped along az (adding ±2π at jumps larger than π) before fitting — then the slope recovers the winding ℓ = d∠F/dφ and intercept the absolute phase offset b. weights (e.g. abs.(F) on the ring) down-weight phase nodes where the angle is ill-defined; nothing ⇒ uniform. Returns the fitted slope, intercept, and the unwrapped series (for plotting).\n\n\n\n\n\n","title":"ElectronDynamicsModels.phase_winding_fit"},{"category":"function","location":"#ElectronDynamicsModels.plot_harmonic_grid","page":"Home","text":"plot_harmonic_grid(maps, x_grid, y_grid; w₀=1, labels, title=\"\", colormap=:jet,\n                   colorrange=harmonic_colorrange, transform=real, colorbar_offset=true,\n                   ncols=3, panelsize=300, outfile=nothing)\n\nDraw a grid of per-component heat-maps for one harmonic — the real part of each component of maps, which is (n_components, Nx, Ny) (e.g. a slice fields_h[k, :, :, :]). Axes are shown in units of w₀ (set it to the beam waist; left as 1 ⇒ raw coordinates, label \"x\"/\"y\"). colormap and colorrange are configurable: colorrange is any data -> (lo, hi) applied per panel (default [harmonic_colorrange] = guarded extrema; pass [symmetric_colorrange] for a diverging map). With colorbar_offset=true (default) each colorbar gets explicit lo/mid/hi ticks — this silences the PlotUtils \"No strict ticks found\" warning on the radiated field's tiny ~1e-17 ranges, and lets Makie's native formatter render the labels as m×10ⁿ (RichText superscripts, since the range spans >4 orders); pass false for O(1) data such as the phase grid (keeps Makie's automatic ticks). ncols sets panels-per-row (3 ⇒ 2×3 for E/B, 2 ⇒ 2×2 for the 4-potential). Saves to outfile if given; returns the Figure.\n\nRequires CairoMakie — using CairoMakie activates the implementation (package extension); calling this without it raises a MethodError.\n\n\n\n\n\n","title":"ElectronDynamicsModels.plot_harmonic_grid"},{"category":"function","location":"#ElectronDynamicsModels.plot_phase_grid","page":"Home","text":"plot_phase_grid(maps, x_grid, y_grid; w₀=1, labels, title=\"\", ncols=3, panelsize=300, outfile=nothing)\n\nLike [plot_harmonic_grid] but plots the phase angle.(maps[c]) of each component on a cyclic :phase colormap over (-π, π) — the (x/w₀, y/w₀, ∠F) view. The panel title still reports each component's peak complex amplitude. Requires CairoMakie (package extension).\n\n\n\n\n\n","title":"ElectronDynamicsModels.plot_phase_grid"},{"category":"function","location":"#ElectronDynamicsModels.plot_phase_polar","page":"Home","text":"plot_phase_polar(maps, x_grid, y_grid; w₀=1, labels, radii, tol, title=\"\", panelsize=300, outfile=nothing)\n\nPolar companion to [plot_phase_with_rings]: for each component of maps ((3, Nx, Ny)), a PolarAxis with angular = azimuth φ and radial = ∠F shifted to [0, 2π) (angle(F)+π), one colour-matched series per test ring R ∈ radii (same pixels as the cartesian view, via ring_pixels). A winding of charge ℓ traces ℓ radial oscillations as φ runs once around. A separate diagnostic from the main phase figure. Requires CairoMakie (package extension).\n\n\n\n\n\n","title":"ElectronDynamicsModels.plot_phase_polar"},{"category":"function","location":"#ElectronDynamicsModels.plot_phase_with_rings","page":"Home","text":"plot_phase_with_rings(maps, x_grid, y_grid; w₀=1, labels, radii, tol, title=\"\",\n                      panelsize=300, outfile=nothing)\n\nCombined per-field phase view for one field type (maps is (3, Nx, Ny) — e.g. the E or B slice fields_h[k, 1:3, :, :] / [k, 4:6, :, :]). One row per component, two columns:\n\nleft — the phase heatmap angle.(maps[c]) on the cyclic :phase colormap over (-π, π), with the test annuli drawn as dashed circles at R ± tol (in w₀ units, to match the axes), one colour per radius;\nright — the azimuthal phase winding: angle(F) of the pixels on each test circle of radius R ∈ radii (a thin annulus, half-width tol, about the grid centre) scattered against azimuth atan(y, x), coloured to match the circles on the left. A vortex of topological charge ℓ shows ℓ phase windings as φ runs once around. Every component gets the phase_winding_fit line overlaid (re-wrapped to ±π), with slope ≈ ℓ and intercept b the phase offset (the longitudinal Eᶻ/Bᶻ wind too — e.g. the ℓ≈3 vortex).\n\nReturns (; fig, fits) where fits[c] (one per component) is (; slope, b) — vectors over radii (NaN where a ring has no pixels), so callers can record the winding/offset in [plot_params]. Requires CairoMakie (package extension).\n\n\n\n\n\n","title":"ElectronDynamicsModels.plot_phase_with_rings"},{"category":"function","location":"#ElectronDynamicsModels.plot_power_spectrum","page":"Home","text":"plot_power_spectrum(freqs, power_spec; ω, labels, marks=nothing, n0=1, colors=…, linestyles=nothing, title=\"\", outfile=nothing)\n\nLog-y plot of per-component power spectra power_spec ((Nf, n), from [power_spectrum]) vs freqs, x-axis in units of the run's radiated fundamental: ω₁ (= ω/2π) when n0 = 1, else the backscattered line ωbs = n0·ω₁ (boosted runs; n0 ≈ 4γ² from the manifest's `backscattern0). Reference gridlines come from Makie's automatic ticks;marks(extracted harmonic bins, in ω₁ units) adds explicit guides. Saves tooutfileif given; returns theFigure`.\n\nRequires CairoMakie — using CairoMakie activates the implementation (package extension).\n\n\n\n\n\n","title":"ElectronDynamicsModels.plot_power_spectrum"},{"category":"method","location":"#ElectronDynamicsModels.power_spectrum-Tuple{AbstractArray{<:Number, 4}}","page":"Home","text":"power_spectrum(cube) -> Matrix{Float64}\n\nPer-component frequency power spectrum of a (N_samples, n_components, Nx, Ny) cube, summed over the screen: out[ν, c] = Σ_pixels |rfft(cube[:,c,:,:], 1)[ν]|². Returns (N÷2+1, n_components); pair with rfftfreq(N_samples, 1/δt) for the frequency axis. One component at a time (memory).\n\n\n\n\n\n","title":"ElectronDynamicsModels.power_spectrum"},{"category":"method","location":"#ElectronDynamicsModels.recommended_n_substeps-Tuple{ObserverScreen}","page":"Home","text":"recommended_n_substeps(screen, c; ω_max = π * c / step(screen.x⁰_samples), rtol = 1e-6)\n\nSuggest an n_substeps value for the fixed-step GPU kernels from the screen's own sampling, with an optional override for the highest relevant frequency.\n\nc is the speed of light in the working units (e.g. getdefault(world.c)). The screen stores the time-like coordinate x⁰ = c·t, so δx⁰ = step(x⁰_samples) is a length and the integration time step is δt = δx⁰ / c.\n\nFixed-step RK4 stepping the retarded-time ODE dτ_r/dx⁰ = f multiplies a mode of angular frequency ω by the stability polynomial |R(iθ)|, θ = ω·δt. Expanding, |R(iθ)| − 1 ≈ θ⁶/144, so at θ = π/2 the per-step amplitude loss is ~7.5%, while at θ ≈ 0.23 it is ~1e-6.  Sub-stepping each saveat interval into n pieces drives θ = ω_max·δt/n down into the accurate range.\n\nω_max is a temporal angular frequency (rad/time) and defaults to the screen's Nyquist angular frequency π·c/δx⁰ = π/δt: a uniform x⁰_samples grid cannot represent anything faster, so resolving the RK4 step to Nyquist guarantees the integration is as accurate as the output grid can carry.  With this default c and δx⁰ cancel and the result is a screen-independent constant (≈14 at rtol = 1e-6).  A caller who knows their top harmonic sits below Nyquist (e.g. ω_max = n_harmonics · 2π·c/λ) can pass it explicitly to take fewer sub-steps.\n\nrtol is the target per-step amplitude error.\n\nnote: Note\nSub-stepping cannot rescue an under-sampled screen.  If the radiation has content above the Nyquist frequency the screen is already aliasing it — that is a δx⁰-too-coarse problem upstream, not one more sub-steps can fix.\n\n\n\n\n\n","title":"ElectronDynamicsModels.recommended_n_substeps"},{"category":"method","location":"#ElectronDynamicsModels.ring_pixels-Tuple{Any, Any, Any}","page":"Home","text":"ring_pixels(x_grid, y_grid, R; tol) -> (idxs, az)\n\nGrid pixels whose distance from the centre lies within tol of radius R (a thin annulus about the grid origin), as a Vector{CartesianIndex{2}} sorted by azimuth, together with those azimuths az = atan(y, x) ∈ (-π, π]. x_grid/y_grid must be centred on the beam axis. Reads a field/phase on a test circle for phase_winding_fit; shared by the phase-ring plots and the LPWA-vs-numeric comparison so both sample identical pixels.\n\n\n\n\n\n","title":"ElectronDynamicsModels.ring_pixels"},{"category":"function","location":"#ElectronDynamicsModels.rpr_capture","page":"Home","text":"rpr_capture(screen; hybrid, exposure = 0.2, sat = 1.0, dump_raw = \"\") -> Matrix{RGBf}\n\nCapture a rendered RPRMakie Screen to an image. On Northstar (hybrid = false) this is plain colorbuffer. On the Hybrid plugins (hybrid = true), where colorbuffer's resolve returns black, it reads the raw HDR framebuffer, normalizes by per-pixel sample count (alpha), applies an exposure-scaled LUMINANCE Reinhard (per-channel compression drags saturated highlights to white) + sRGB gamma, then an optional post-tonemap saturation sat in gamma space (≈1.25 recovers Northstar's per-channel photolinear vibrancy without blowing highlights). dump_raw serializes the raw HDR buffer for offline regrading.\n\n\n\n\n\n","title":"ElectronDynamicsModels.rpr_capture"},{"category":"function","location":"#ElectronDynamicsModels.rpr_enable_multiframe!","page":"Home","text":"rpr_enable_multiframe!()\n\nWork around the HybridPro rprSceneClear segfault when rendering many frames in one process: RPRMakie's singleton Context releases the previous context on every new Screen, and HybridPro crashes in the release. After this call, new contexts are created non-singleton — they LEAK (measured ~1.7 GB VRAM/frame on heavy scenes) but never tear down. Bound the leak by chunking long frame ranges across processes (~16 frames/process on a 48 GB card).\n\n\n\n\n\n","title":"ElectronDynamicsModels.rpr_enable_multiframe!"},{"category":"function","location":"#ElectronDynamicsModels.rpr_tune!","page":"Home","text":"rpr_tune!(screen; quality = nothing, denoiser = nothing, ray_depth = nothing)\n\nSet HybridPro context parameters on an RPRMakie Screen (call right after constructing it; Hybrid plugins only — Northstar uses different knobs):\n\nquality: \"low\" | \"medium\" | \"high\" | \"ultra\" — RPRCONTEXTRENDER_QUALITY (0x1001), gates HybridPro's algorithmic shortcuts.\ndenoiser: \"none\" | \"svgf\" | \"asvgf\" | \"ml\" — RPRCONTEXTPT_DENOISER (0x102D; ml needs RadeonImageFilters libs the jll doesn't ship).\nray_depth: Int — maxrecursion + refraction/glossyrefraction depths (glossy capped at 8). Nested translucent shells want 12-16: rays that exhaust the preset budget terminate BLACK. Probed: HybridPro ACCEPTS the recursion/diffuse/glossy/refraction depth family and REJECTS shadow depth, Russian roulette, sampler type, and adaptive sampling.\n\n\n\n\n\n","title":"ElectronDynamicsModels.rpr_tune!"},{"category":"method","location":"#ElectronDynamicsModels.screen_observables-Tuple{Any, Any}","page":"Home","text":"screen_observables(field, screen; ε₀) -> NamedTuple\n\nDerive radiation diagnostics from the accumulated field = (; E, B) (the output of [accumulate_field]) as functions of the observer-time sample index — all following from the Faraday tensor through the electromagnetic stress-energy tensor Tᵘᵛ:\n\nS              — Poynting vector,             (N, 3, Nx, Ny)\nenergy_density — u = ½ε₀(E² + c²B²),        (N, Nx, Ny)\nLz_density     — z angular-momentum flux dens, (N, Nx, Ny)\nenergy_total   — ∫∫ Sᶻ dA dt (energy through the screen)\nLz_total       — ∫∫ Lz_density dA dt\n\nEach pixel's field is reassembled into the Faraday tensor faraday, then T^{μν} is formed covariantly via stress_energy; S, energy density, and the angular-momentum flux angular_momentum_flux_z are read off T.\n\n\n\n\n\n","title":"ElectronDynamicsModels.screen_observables"},{"category":"method","location":"#ElectronDynamicsModels.screen_spectrum-Tuple{Any, Any}","page":"Home","text":"screen_spectrum(field, screen; ε₀, bins = nothing) -> NamedTuple\n\nFrequency-domain radiation diagnostics from the accumulated field = (; E, B).\n\nThe FFT is linear, so it acts on the fields E, B (not on the quadratic observables); every component of the stress-energy tensor then has a frequency-domain image T̃(ω) that is a bilinear cross-spectral density Re[F̃*(ω) F̃(ω)] of the transformed field — assembled here per kept bin:\n\nfreqs     — frequencies (1/time units) at the kept bins\nE_ω, B_ω  — complex field spectra,         (n_bins, 3, Nx, Ny)\nenergy_ω  — ½ε₀(|Ẽ|² + c²|B̃|²),          (n_bins, Nx, Ny)\nS_ω       — Re[Ẽ*×B̃] / μ₀,               (n_bins, 3, Nx, Ny)\nLz_ω      — x T̃ᶻʸ − y T̃ᶻˣ,               (n_bins, Nx, Ny)\n\nbins selects which rfft frequency indices to keep (e.g. harmonic bins located via rfftfreq); nothing keeps all. Pass a small bins when only a few frequencies are needed — the complex E_ω, B_ω are full grid-sized per bin, so keeping every bin materialises arrays as large as E itself.\n\nThe transform is blocked over screen rows, so the transient complex array is (N÷2+1, 3, Nx), never the full grid. Spectra use the δt-scaled rfft (Ẽ ≈ ∫E e^{iωt} dt); the phase is independent of this scaling, so a phase map of a component at bin m is simply angle.(E_ω[m, i, :, :]).\n\n\n\n\n\n","title":"ElectronDynamicsModels.screen_spectrum"},{"category":"method","location":"#ElectronDynamicsModels.stress_energy-Tuple{Any, Any, Any}","page":"Home","text":"stress_energy(F, g, μ₀) -> SMatrix{4,4}\n\nElectromagnetic stress-energy tensor T^{μν} from the upper-index Faraday tensor F, metric g, and vacuum permeability μ₀, in the (+,−,−,−) convention (F^{0i} = −Eⁱ/c):\n\nM = F g Fᵀ                       # M^{μν} = F^{μα} g_{αβ} F^{νβ}\nF_{αβ}F^{αβ} = tr(g M)           # = 2(B² − E²/c²)   (uses Fᵀ = −F)\nT^{μν} = (1/μ₀) [ −M^{μν} + ¼ g^{μν} (F_{αβ}F^{αβ}) ]\n\nThe sign is fixed so T⁰⁰ = ½ε₀(E²+c²B²) > 0 (energy density) and Sⁱ = c·T⁰ⁱ (Poynting).  Written with StaticArrays matrix ops so one function serves both the symbolic EMFieldDynamics (via @SMatrix of Num) and the numeric reduction in screen_observables.\n\nReferences: Landau & Lifshitz \"Classical Theory of Fields\" §33; Jackson §12.10.\n\n\n\n\n\n","title":"ElectronDynamicsModels.stress_energy"},{"category":"method","location":"#ElectronDynamicsModels.symmetric_colorrange-Tuple{Any}","page":"Home","text":"symmetric_colorrange(data) -> (-m, m)\n\nA color range symmetric about zero (m = maximum(abs, data)), for diverging colormaps like :seismic; guarded against a degenerate panel. Pass as plot_harmonic_grid's colorrange.\n\n\n\n\n\n","title":"ElectronDynamicsModels.symmetric_colorrange"},{"category":"method","location":"#ElectronDynamicsModels.trajectory_interpolants-Tuple{SciMLBase.EnsembleSolution}","page":"Home","text":"trajectory_interpolants(ensemble_sol::EnsembleSolution)\n\nReturn a vector of TrajectoryInterpolant objects, which interpolate the trajectories in a fast and thread safe manner.\n\n\n\n\n\n","title":"ElectronDynamicsModels.trajectory_interpolants"},{"category":"method","location":"#ElectronDynamicsModels.trajectory_span_for_window-Tuple{Any, Any, AbstractRange, Any, Any, Any}","page":"Home","text":"trajectory_span_for_window(τi, τf, x⁰_samples, Z, half_width, Rmax; c, margin_samples = 2, stretch = 1)\n\nProper-time span (τ_lo, τ_hi) ⊇ (τi, τf) a trajectory solve must cover so that every pixel of the screen sees every electron at EVERY sample of x⁰_samples, i.e. the field kernels' strict- interior slot range k_start:k_end is 1:N_samples for every (electron, pixel) pair. The history must begin margin_samples before the window opens at the latest-arriving pair (the corner pixel / far-rim electron, see observer_window_start) and must last until the EARLIEST retarded image of its end — the axis pixel, where the arrival is the light-front coordinate x⁰ − x³ plus Z — reaches the last sample. stretch is the proper time per unit of light-front advance: 1 for electrons at rest, γ(1+β) for force-free flight toward the screen (x⁰ − x³ = c·τ/(γ(1+β))). The physics span (τi, τf) is never shrunk; the extension only adds history where the electron moves freely.\n\n\n\n\n\n","title":"ElectronDynamicsModels.trajectory_span_for_window"},{"category":"method","location":"#ElectronDynamicsModels.window_coverage-Tuple{AbstractVector{<:TrajectoryInterpolant}, ObserverScreen}","page":"Home","text":"window_coverage(trajs, screen; recount = true) -> NamedTuple\n\nCheck on the host that the observer window screen.x⁰_samples lies inside every pixel's arrival window for every electron — the condition under which the GPU field kernels (accumulate_field with GPUKernelNewton / GPUKernelRK4) execute all N_samples slots per (electron, pixel), the cube receives every electron's full history, and the nominal work N·N_samples·Nx·Ny is exact. Uses the kernels' own window arithmetic (_window_edge, same floor/ceil slot formulas) on the host spline, evaluated at the four corner pixels and the pixel nearest to the electron, which bound the arrival offsets exactly (see the file header); clipped electrons are recounted over every pixel when recount = true.\n\nFields of the result:\n\nok::Bool — every electron fully covered.\nslots_nominal, slots_executed, slot_fill = slots_executed / slots_nominal (slots_executed is missing for a clipped run with recount = false).\nelectrons_clipped, slots_dropped = slots_nominal - slots_executed.\nlead_margin_samples — samples the window could start earlier before the first pixel is clipped at the start (negative = already clipped by that many samples at the worst pixel); tail_margin_samples — same for the end of the window (a negative value means some trajectory ends before the window does, at some pixel).\nworst_electron — index with the smallest tail margin.\nper_electron::Vector of (; k_start_max, k_end_min, lead_margin, tail_margin, slots_executed).\n\nCost: ten spline evaluations per electron (milliseconds for 10⁴ electrons), plus 2·Nx·Ny per clipped electron. Rounding note: floor/ceil at an exact sample boundary can differ by one slot between this host evaluation and a device launch; that cannot flip ok except at a measure-zero boundary.\n\n\n\n\n\n","title":"ElectronDynamicsModels.window_coverage"},{"category":"module","location":"#GPUDiagnostics.GPUDiagnostics","page":"Home","text":"GPUDiagnostics\n\nVendor-neutral runtime diagnostics for KernelAbstractions code. Every entry point dispatches on the KA Backend; the CUDA.jl / AMDGPU.jl package extensions supply the vendor methods, loaded on demand when the vendor package is in the session, and the KA CPU backend gets host fallbacks so the plumbing (and its tests) run without a GPU.\n\nDevice API — gpu_device_count, gpu_device, gpu_device!, gpu_name, gpu_arch, gpu_sm_count, gpu_max_threads_per_sm, gpu_memory_info, gpu_power, gpu_utilization, thread_fill_occupancy. KernelAbstractions has no device management of its own.\nDevice-event kernel timing — gpu_event / gpu_elapsed on the task-local launch stream, and LaunchTimer + launch_times for one event pair per launch: the only kernel clock that works when launches are queued asynchronously (a host clock measures enqueue latency).\nTelemetry — gpu_sample is one snapshot of a device (power / utilization / VRAM, plus NVIDIA GPM hardware counters where available: ACHIEVED SM occupancy, FP64 / FP32 / tensor pipe and DRAM-bandwidth utilization, PCIe / NVLink traffic); with_gpu_sampler runs a function while a child process takes that sample per device per tick into a TSV, and gpu_telemetry_stats reduces the resulting GPUTelemetry column table. In-process sampling wedges behind a backed-up kernel stream or is suspended by Julia's GC/timer coupling; a child is immune.\nMeasured FP64 peak — measure_peak_fp64_flops / gpu_peak_fp64_flops: a dependent-FMA-chain kernel gives the attainable vector FP64 rate of the device at the clocks it actually holds (never routed to matrix/tensor units); BLAS peakflops on the CPU backend.\nCompile-time resource report — compiled_kernels inventories the kernels this process compiled (from the vendor's kernel cache, so closures inside driver functions are reachable too) and kernel_resources reports registers, spill/stack and shared (LDS) memory, and the runtime's theoretical occupancy for the block size actually launched — plus the SGPR/VGPR/ spill counts from the AMD ISA dump.\nStatic instruction mix — kernel_instruction_mix counts the compiled instruction stream (AMD ISA / NVIDIA SASS) by class — FP64 fma/add/mul/transcendental/other/packed, FP32, integer, scalar, memory, LDS, control, waits — for the whole kernel and for each loop of its control-flow graph (the hot per-slot loop in particular), natively or cross-compiled for a target that is not present (target = \"gfx942\", \"sm_90\"); kernel_ir_mix counts the same job's optimized LLVM IR by typed opcode (the arithmetic before backend contraction); fp64_issue_floor turns a per-slot FP64 count and the measured FP64 rate into the FP64-pipe time floor per launch.\nAMD hardware counters (rocprofv3) — rocprof_command wraps a process in a rocprofv3 counter collection (ROCPROF_COUNTER_SETS: instruction issue, wave residency, the L1 pipe, FP64, L2 — the sets that fit one pass on gfx942, and the one that does not), rocprof_counters parses its CSV into per-dispatch values of one kernel, and rocprof_derived / rocprof_summary / rocprof_manifest_section reduce them: medians with spreads, per-slot instruction counts, unit-busy fractions and achieved occupancy with the normalisation rocprofv3's own derived metrics use (die-summed GRBM_GUI_ACTIVE, quad-cycle SQ counters). Pure Julia, no GPU needed to parse.\n\n\n\n\n\n","title":"GPUDiagnostics.GPUDiagnostics"},{"category":"constant","location":"#GPUDiagnostics.AMD_RULES","page":"Home","text":"AMD_RULES :: Vector{Pair{Regex, Symbol}}\n\nOrdered classification rules of instruction_mix for AMD ISA mnemonics (GCN / RDNA / CDNA), matched after the _e32/_e64/_dpp/_sdwa encoding suffix is dropped. Same contract as SASS_RULES: first match wins, the final rule is the :unclassified catch-all, and the vector is mutable for session overrides.\n\n\n\n\n\n","title":"GPUDiagnostics.AMD_RULES"},{"category":"constant","location":"#GPUDiagnostics.IR_CLASSES","page":"Home","text":"IR_CLASSES\n\nOperation classes of kernel_ir_mix: fp64_fma (llvm.fma.f64, llvm.fmuladd.f64), fp64_add (fadd/fsub double), fp64_mul, fp64_div, fp64_neg, fp64_sqrt (llvm.sqrt.f64), fp64_cmp (fcmp on doubles), fp64_cvt (conversions to/from double), fp64_intrinsic (other llvm.* intrinsics returning or taking double: fabs, floor, minnum, …), fp64_contract (the subset of the double fadd/fsub/fmul that carry LLVM's contract fast-math flag — Julia lowers muladd to a contract-flagged pair rather than an fmuladd intrinsic, and the backend fuses them at instruction selection; informational, not added to the fp64 total), fp32 (the same on float/half), int (integer arithmetic, logic, shifts, compares, integer casts, selects), mem_load, mem_store, mem_atomic, call (non-intrinsic calls), control (br, switch, ret, unreachable), other (phi, getelementptr, allocas, pointer casts, extract/insertvalue, …).\n\n\n\n\n\n","title":"GPUDiagnostics.IR_CLASSES"},{"category":"constant","location":"#GPUDiagnostics.MIX_CLASSES","page":"Home","text":"MIX_CLASSES\n\nInstruction classes of kernel_instruction_mix (vendor-neutral; the manifest keys are kernel_mix_<class>):\n\nfp64_fma, fp64_add, fp64_mul — FP64 fused multiply-add (DFMA / v_fma_f64, v_fmac_f64, v_div_fmas_f64), add/sub, mul;\nfp64_trans — FP64 reciprocal / rsqrt / sqrt seeds (MUFU.RCP64H, MUFU.RSQ64H / v_rcp_f64, v_rsq_f64, v_sqrt_f64);\nfp64_other — every other FP64-pipe instruction: compares (DSETP / v_cmp_*_f64), min/max, conversions to/from FP64, rounding, the division helpers (v_div_scale_f64, v_div_fixup_f64, v_ldexp_f64);\nfp64_packed — CDNA packed FP64 (v_pk_fma_f64, v_pk_mul_f64, v_pk_add_f64: two lanes of FP64 work per instruction; NVIDIA has no equivalent);\nfp32 — FP32 and FP16 arithmetic (FFMA/FADD/FMUL/MUFU.RCP/HFMA2 / v_*_f32, v_*_f16);\nint — vector integer / logic / move / select / compare (IMAD, IADD3, LOP3, SHF, ISETP, MOV, SEL, PRMT … / v_add_co_u32, v_cndmask_b32, v_mov_b32, v_readlane_b32, v_accvgpr_* …);\nsalu — scalar / uniform-datapath instructions (AMD s_* ALU; NVIDIA U* uniform ops, R2UR, S2UR);\nmem_load, mem_store, mem_atomic — vector-memory loads, stores, atomics (global, flat, local/scratch, buffer);\nsmem — scalar / constant-bank loads (s_load_*; LDC, LDCU, ULDC);\nlds — shared memory (ds_*; LDS, STS, ATOMS, LDSM);\ncontrol — branches, calls, returns, exit, convergence (BSSY/BSYNC/WARPSYNC/BREAK) and barriers;\nwait — explicit dependency waits and software-scheduled stalls (s_waitcnt*, s_wait_*, RDNA3's s_delay_alu; DEPBAR);\nnop — no-ops that occupy issue slots (s_nop, v_nop; NOP — ptxas pads FP64 dependency latency with them on the 64:1-rate consumer parts, so they can be a large share of a loop);\nother — scheduling hints (s_clause, s_set_inst_prefetch_distance), cache maintenance and fences (buffer_gl*_inv, buffer_wbl2, MEMBAR, ERRBAR, CCTL), special registers (S2R, CS2R), shuffles/votes, and anything unrecognised (see the opcodes histogram of the report).\n\n\n\n\n\n","title":"GPUDiagnostics.MIX_CLASSES"},{"category":"constant","location":"#GPUDiagnostics.ROCPROF_COUNTER_SETS","page":"Home","text":"ROCPROF_COUNTER_SETS\n\nNamed rocprofv3 --pmc counter sets for rocprof_command(…; counters = :name). Each set is ONE pass (rocprofv3 collects a set per dispatch; the hardware's counter capacity is the limit — a set that exceeds it aborts the profiled process, see below). What each answers, and what rocprof_derived computes from it:\n\n:sq_issue — instruction issue per wave, by class: SQ_INSTS_VMEM_RD/_WR, _SMEM, _SALU, _BRANCH, _VALU_INT64, _VALU (+ GRBM_GUI_ACTIVE). → insts_per_slot_<class> (× wave size / slots): the dynamic instruction mix per slot to hold against the static kernel_instruction_mix of the hot loop; is the kernel load-, integer- or branch-heavy.\n:sq_waves — wave residency and what waves do: SQ_WAVES, SQ_BUSY_CYCLES, SQ_WAVE_CYCLES, SQ_ACTIVE_INST_ANY/_VALU, SQ_WAIT_INST_ANY, SQ_WAIT_ANY (+ GRBM_GUI_ACTIVE). → resident_waves, waves_per_cu, occupancy (achieved, vs kernel_occupancy at compile time), wave_wait_frac (waiting for anything), wave_wait_inst_frac (waiting for an instruction to ISSUE — the difference to wave_wait_frac is the dependency / memory-latency wait), wave_active_inst_frac, wave_active_valu_frac, sq_busy: latency-bound (waves mostly waiting on data) vs issue-bound (waiting on the arbiter).\n:l1_pipe — the vector-memory pipe: TA_TA_BUSY_sum, TD_TD_BUSY_sum, TCP_PENDING_STALL_CYCLES_sum, TCP_TOTAL_READ_sum, TCP_TCC_READ_REQ_sum, TCP_TOTAL_CACHE_ACCESSES_sum (+ GRBM_GUI_ACTIVE). → ta_busy, td_busy, tcp_pending_stall (fractions of the dispatch, per CU), l1_miss (L1 → L2 read requests per cache access): is the address/data path saturated, does the working set fit L1.\n:fp64 — FP64 VALU issue: SQ_INSTS_VALU_FMA_F64, _ADD_F64, _MUL_F64, _TRANS_F64 (+ GRBM_GUI_ACTIVE). → insts_per_slot_valu_fma_f64 etc. and fp64_flop_per_slot (2·FMA + ADD + MUL + TRANS, per slot): the hardware's own FLOP count per slot to hold against the algorithmic [flops].flop_per_slot.\n:l2 — the L2 (TCC) and what leaves the die: TCC_HIT_sum, TCC_MISS_sum, TCC_EA0_RDREQ_sum (L2 → fabric read requests), TCC_EA0_RDREQ_DRAM_sum (of those, to HBM) (+ GRBM_GUI_ACTIVE). → l2_hit, l2_dram_read_frac; the _median values / slots give the L2 requests and HBM reads per slot: does the working set live in L2 or in HBM.\n\nVerified on gfx942 (MI300X VF, ROCm 7.2.4, rocprofv3 1.1.0): :sq_issue, :sq_waves and :l1_pipe collect in one pass on the production field kernel; :fp64 and :l2 on a test kernel (c = a + b * 1.5 over 2^22 doubles = 65 536 waves: exactly 1.00 ADD_F64 + 1.00 MUL_F64 per element from × 64 / slots, and TCC hits / misses / fabric / HBM reads all populated). The TCC block's capacity is FOUR counters per pass: adding TCC_REQ_sum as a fifth — with or without TCC_READ_sum as a sixth, the original six-counter L2 set — makes rocprofv3 log \"Request exceeds the capabilities of the hardware to collect\", abort with signal 6 and leave the profiled child hung until timeout -k kills it (exit 137; hence the timeout in rocprof_command). Sets are Vector{String} so a caller can pass its own list instead of a name.\n\n\n\n\n\n","title":"GPUDiagnostics.ROCPROF_COUNTER_SETS"},{"category":"constant","location":"#GPUDiagnostics.SASS_RULES","page":"Home","text":"SASS_RULES :: Vector{Pair{Regex, Symbol}}\n\nOrdered classification rules of instruction_mix for NVIDIA SASS mnemonics (matched against the full OPC.MOD… opcode) and AMD ISA mnemonics (matched after the _e32/_e64/ _dpp/_sdwa encoding suffix is dropped). The first matching rule wins; the final r\"\" rule is the catch-all :unclassified (counted as other, listed in unclassified_opcodes). The vectors are mutable: pushfirst!(SASS_RULES, r\"^MYOP\" => :int) overrides for a session.\n\n\n\n\n\n","title":"GPUDiagnostics.SASS_RULES"},{"category":"type","location":"#GPUDiagnostics.CompiledKernel","page":"Home","text":"CompiledKernel\n\nOne entry of compiled_kernels: a kernel this process compiled for the vendor runtime. name is the Julia function the kernel was generated from (gpu__forindices_global! for an AcceleratedKernels foreachindex, gpu_<kernel> for a KernelAbstractions @kernel), signature the argument-type tuple as a string (closure types carry the enclosing function's name on Julia ≥ 1.12), workgroup_size the static KernelAbstractions workgroup size read from the signature (nothing when dynamic or not a KA kernel), and kernel the vendor kernel object (CUDA.jl HostKernel, AMDGPU.jl HIPKernel).\n\n\n\n\n\n","title":"GPUDiagnostics.CompiledKernel"},{"category":"type","location":"#GPUDiagnostics.GPUTelemetry","page":"Home","text":"GPUTelemetry\n\nColumn table returned by with_gpu_sampler: columns ([:t_rel_s, :device, metric columns…] as declared by the child), samples::Matrix{Float64} (one row per device per tick, NaN where a metric is not reported), ticks (sample rounds = rows of the first device), dt and window (requested cadence / sampled window, s), first_sample_s (child startup lag), starved (ticks ≪ window/dt ⇒ stats unreliable), trace (the TSV, or nothing) and counters. Index a column by symbol: telem[:compute_util]; haskey(telem, :fp64_util).\n\n\n\n\n\n","title":"GPUDiagnostics.GPUTelemetry"},{"category":"type","location":"#GPUDiagnostics.LaunchTimer","page":"Home","text":"LaunchTimer()\n\nCollects a device-event pair per kernel launch (see gpu_event), keyed by the 1-based device the launch ran on. A launch loop instruments itself with launch_lane once per call and launch_tick / launch_tock! around each launch; read back with launch_times once the loop has returned. Safe to share across per-device tasks (pushes are locked; events are per-stream). Passing nothing instead of a timer records nothing and costs nothing.\n\nlane = launch_lane(timer, backend)\nfor item in work\n    e0 = launch_tick(timer, backend)\n    launch_kernel!(item, backend)\n    launch_tock!(timer, lane, backend, e0)\nend\nlaunch_times(timer)   # Dict(device => [seconds per launch, …])\n\n\n\n\n\n","title":"GPUDiagnostics.LaunchTimer"},{"category":"type","location":"#GPUDiagnostics.RocprofCounters","page":"Home","text":"RocprofCounters\n\nPer-dispatch hardware counters of ONE kernel from a rocprofv3 run, built by rocprof_counters: counters (names), dispatch_ids, values (dispatches × counters), duration_s (from the dispatch timestamps), resources (grid_size, workgroup_size, vgpr_count, agpr_count, sgpr_count, lds_bytes, scratch_bytes of the kernel), device (name, product, n_cu, n_xcd, n_se, n_simd, wave_size, max_waves_per_cu from the agent info, 0 = unknown), slots (per dispatch, as given by the caller, or nothing), kernel (the matched name) and dispatches (every dispatch of the run: id, kernel, duration_s). Index a counter by name: rc[\"SQ_WAVES\"] (a vector over dispatches); haskey(rc, \"SQ_WAVES\"); median(rc, \"SQ_WAVES\") via rocprof_median.\n\n\n\n\n\n","title":"GPUDiagnostics.RocprofCounters"},{"category":"type","location":"#GPUDiagnostics.SamplerSource","page":"Home","text":"SamplerSource\n\nPer-device sampling state. Subtypes implement sample!(source) -> NamedTuple (metric name ⇒ Float64, NaN for a metric the device does not report) and carry a device::Int field (the 1-based vendor ordinal); close!(source) releases resources (default no-op). Sources are built from string specs by sampler_source: <kind>:<payload>, dispatched on Val(kind).\n\n\n\n\n\n","title":"GPUDiagnostics.SamplerSource"},{"category":"method","location":"#GPUDiagnostics._natural_loops-Tuple{Vector{GPUDiagnostics.MixBlock}}","page":"Home","text":"_natural_loops(blocks) -> Vector{(header, body, depth)}\n\nNatural loops of the block graph: one per loop header (back edges u → h with h dominating u, bodies merged per header), with the nesting depth (1 = outermost). Returns block indices.\n\n\n\n\n\n","title":"GPUDiagnostics._natural_loops"},{"category":"method","location":"#GPUDiagnostics.compiled_kernels-Tuple{KernelAbstractions.Backend}","page":"Home","text":"compiled_kernels(backend; pattern = nothing) -> Vector{CompiledKernel}\n\nInventory of the kernels THIS PROCESS has compiled for backend's vendor runtime, from the vendor package's kernel-instance cache (every @cuda/@roc/KernelAbstractions launch lands there on its first call). pattern (a Regex or AbstractString) keeps only entries whose name or signature matches — the way to pick a production kernel that is a closure inside its driver: compiled_kernels(backend; pattern = r\"_my_driver!\"). The CPU backend compiles nothing and returns an empty vector. Pass entries to kernel_resources.\n\n\n\n\n\n","title":"GPUDiagnostics.compiled_kernels"},{"category":"method","location":"#GPUDiagnostics.fp64_issue_floor-Tuple{Any}","page":"Home","text":"fp64_issue_floor(mix; n_slots, peak_fp64_flops, kernel_time_s = nothing, scope = :hot_loop) -> NamedTuple\n\nThe time the FP64 pipe alone needs for a launch, from a STATIC instruction count and a MEASURED issue rate: fp64_per_slot FP64 instructions (the six FP64 classes of mix's hot_loop — scope = :total uses the whole-kernel count instead) × n_slots executions (hot-loop iterations summed over all threads; e.g. pixels × samples when every slot is inside the window) ÷ the device's FP64 lane-instruction rate, taken as peak_fp64_flops / 2 (measure_peak_fp64_flops runs an FMA chain: 2 FLOP per instruction). floor_s is a lower bound on the launch time on an FP64-issue-bound device; fp64_issue_fraction = floor_s / kernel_time_s (when a measured launch time is given) is the fraction of the launch during which the FP64 pipe was provably issuing.\n\nAssumptions, stated in assumptions: every FP64 instruction issues at the FMA rate (adds and multiplies do; MUFU/v_rcp_f64 seeds and packed CDNA ops do not, so the floor errs low); the static hot-loop count is one pass through the loop with nested loops counted once — an inner loop of n iterations (a Newton solve, say) adds (n − 1) × its own count per slot, so the floor is again low by that much; nothing outside the hot loop (per-thread setup) is counted.\n\n\n\n\n\n","title":"GPUDiagnostics.fp64_issue_floor"},{"category":"function","location":"#GPUDiagnostics.gpu_arch","page":"Home","text":"gpu_arch(backend) -> String\n\nArchitecture tag of the current device: the compute capability (\"9.0\") on NVIDIA, the gfx name without feature suffixes (\"gfx942\") on AMD. Provenance only.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_arch"},{"category":"function","location":"#GPUDiagnostics.gpu_device","page":"Home","text":"gpu_device(backend) -> Int\n\n1-based index of the current device (this common API is 1-based across vendors).\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_device"},{"category":"function","location":"#GPUDiagnostics.gpu_device!","page":"Home","text":"gpu_device!(backend, i) -> Int\n\nMake GPU i (1-based) current; returns the previously-current index. One Julia task per device\n\ngpu_device! is how multi-device work sharding pins each shard to a GPU.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_device!"},{"category":"function","location":"#GPUDiagnostics.gpu_device_count","page":"Home","text":"gpu_device_count(backend) -> Int\n\nNumber of GPUs the vendor runtime exposes for backend.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_device_count"},{"category":"function","location":"#GPUDiagnostics.gpu_elapsed","page":"Home","text":"gpu_elapsed(start, stop) -> Float64\n\nSeconds between two events from gpu_event. Waits for stop to complete first, so it is safe to call before the stream is otherwise synchronized.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_elapsed"},{"category":"function","location":"#GPUDiagnostics.gpu_event","page":"Home","text":"gpu_event(backend) -> event\n\nRecord a timestamp event on the CURRENT TASK's stream (the one KernelAbstractions launches on) and return it. Pair two with gpu_elapsed. The CPU backend returns time_ns() — its kernels are synchronous, so the host clock is the kernel clock.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_event"},{"category":"function","location":"#GPUDiagnostics.gpu_max_threads_per_sm","page":"Home","text":"gpu_max_threads_per_sm(backend) -> Int\n\nMax resident threads per SM/CU — with gpu_sm_count, the device's total resident-thread capacity (the denominator of thread-fill occupancy).\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_max_threads_per_sm"},{"category":"function","location":"#GPUDiagnostics.gpu_memory_info","page":"Home","text":"gpu_memory_info(backend) -> @NamedTuple{total, free, used}\n\nCurrent-device global memory, in bytes.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_memory_info"},{"category":"function","location":"#GPUDiagnostics.gpu_name","page":"Home","text":"gpu_name(backend) -> String\n\nMarketing name of the current device.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_name"},{"category":"method","location":"#GPUDiagnostics.gpu_peak_fp64_flops-Tuple{KernelAbstractions.Backend}","page":"Home","text":"gpu_peak_fp64_flops(backend) -> Float64\n\nAttainable vector (non-matrix/tensor) FP64 peak of the current device in FLOP/s, MEASURED on the device with the dependent-FMA-chain probe of measure_peak_fp64_flops (any KernelAbstractions backend, no per-architecture table) — or, on the CPU backend, BLAS LinearAlgebra.peakflops. The denominator of a percent-of-peak figure for scalar FP64 kernels; the matrix/tensor peak would be the wrong yardstick. Costs ~1.5 s of device time per call.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_peak_fp64_flops"},{"category":"function","location":"#GPUDiagnostics.gpu_power","page":"Home","text":"gpu_power(backend) -> Float64\n\nInstantaneous board power draw of the current device, in Watts. A cheap live proxy for compute saturation — NOT occupancy: low power + high SM-utilization ⇒ latency-bound.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_power"},{"category":"function","location":"#GPUDiagnostics.gpu_sample","page":"Home","text":"gpu_sample(backend, device = gpu_device(backend); counters = :auto) -> NamedTuple\n\nOne telemetry sample of device (1-based vendor ordinal), in-process: power_W, compute_util, mem_util, vram_used_B (fractions in [0, 1]; NaN when the device does not expose a counter) and, on NVIDIA GPUs with GPM (Hopper and newer) when counters = :auto, sm_util, sm_occupancy (ACHIEVED), fp64_util, dram_bw_util, fp32_util, fp16_util, tensor_util, int_util, pcie_tx_MiBps, pcie_rx_MiBps, nvlink_rx_MiBps, nvlink_tx_MiBps. Interval metrics (GPM) cover the time since the previous gpu_sample of that device (the first call takes a 100 ms interval). This is exactly what the sampler child calls per tick; for a time series while a kernel runs use with_gpu_sampler — an in-process loop is suspended by Julia's GC/timer coupling while the host allocates.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_sample"},{"category":"method","location":"#GPUDiagnostics.gpu_sampler_sources-Tuple{KernelAbstractions.Backend, AbstractVector{<:Integer}, Symbol}","page":"Home","text":"gpu_sampler_sources(backend, device_ids, counters) -> (specs, packages)\n\nVendor hook (implemented by the CUDA.jl / AMDGPU.jl extensions): resolve the (1-based) device_ids into source specs a child process can open WITHOUT the vendor runtime — NVML uuids on NVIDIA (nvml:GPU-<uuid>=<device>; the child loads the listed packages, i.e. CUDA.jl, for the NVML bindings only), amdgpu sysfs paths on AMD (sysfs:…, no packages). counters is :auto or :none. The vendor runtime is touched only here, in the parent.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_sampler_sources"},{"category":"function","location":"#GPUDiagnostics.gpu_sm_count","page":"Home","text":"gpu_sm_count(backend) -> Int\n\nStreaming-multiprocessor (CU on AMD) count of the current device.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_sm_count"},{"category":"method","location":"#GPUDiagnostics.gpu_telemetry_stats-Tuple{GPUDiagnostics.GPUTelemetry}","page":"Home","text":"gpu_telemetry_stats(telem; busy_column = :compute_util, busy_threshold = 0.5) -> Dict{String, Any}\n\nReduce a GPUTelemetry over all devices' rows, skipping NaN entries per column: for every metric column <col>_mean, <col>_peak and <col>_busy_mean — the mean over rows whose busy_column is ≥ busy_threshold, i.e. the kernel-active part of the window without the idle JIT / upload / drain phases diluting it (the number to hold against a kernel's theoretical occupancy) — plus samples (ticks) and busy_samples (rows). Empty when there are no rows.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_telemetry_stats"},{"category":"function","location":"#GPUDiagnostics.gpu_utilization","page":"Home","text":"gpu_utilization(backend) -> @NamedTuple{compute, memory}\n\nCurrent-device utilization, each a 0–1 fraction (fraction of recent time the engine was busy).\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_utilization"},{"category":"method","location":"#GPUDiagnostics.instruction_mix-Tuple{AbstractString, Symbol}","page":"Home","text":"instruction_mix(text, vendor::Symbol) -> NamedTuple\n\nStatic instruction mix of a machine-code listing — the pure part of kernel_instruction_mix: vendor is :amd (LLVM AMDGPU ISA text as code_native prints it) or :nvidia (SASS as nvdisasm --print-code prints it). Fields:\n\ntotal — instructions in the listing (every function, every path, each counted once);\ncounts — a NamedTuple over MIX_CLASSES; fp64 its FP64 sum (all six FP64 classes, packed included);\nopcodes — the raw mnemonic histogram (Dict{String, Int}), for drilling into other;\nunclassified, unclassified_opcodes, coverage — instructions no rule of SASS_RULES / AMD_RULES but the catch-all matched (they count as other), their histogram, and 1 − unclassified / total. Deliberate other rules (fences, cache maintenance, special registers, hints) are classified, not unclassified;\nblocks — basic blocks parsed;\nloops — the natural loops of the control-flow graph, largest first, each with header (label), depth (1 = outermost), blocks, total and counts (everything inside the loop, nested loops included) and exclusive_total / exclusive_counts (the loop's own blocks only). Trivial loops (≤ 2 instructions of pure control flow, e.g. the trap spin after EXIT) are dropped;\nhot_loop — the depth-1 loop with the most instructions (nothing when there is none): for a one-work-item-per-thread kernel with an inner per-slot loop this is that loop, and its counts are the static per-slot instruction floor (nested loops counted once);\nhot_loop_confidence — :high when the LLVM assembly printer's own loop annotations are present and agree with the CFG analysis (AMD), :medium when the CFG has a single dominant outer loop (≥ 2× the next), :low otherwise, :none without loops;\nllvm_loops_agree — true/false when LLVM annotations were present, else nothing.\n\n\n\n\n\n","title":"GPUDiagnostics.instruction_mix"},{"category":"method","location":"#GPUDiagnostics.kernel_instruction_mix-Tuple{KernelAbstractions.Backend, CompiledKernel}","page":"Home","text":"kernel_instruction_mix(backend, ck::CompiledKernel; target = nothing, dump = nothing) -> NamedTuple\n\nStatic instruction mix of a compiled kernel (see compiled_kernels): the vendor disassembly of the kernel — the AMD ISA text of code_native, the SASS of CUDA.jl's bundled nvdisasm on the cubin — counted by MIX_CLASSES, with the loop nest recovered from the control-flow graph and the hot loop's counts. All fields of instruction_mix, plus name, signature, target (the ISA the counted code was compiled for: gfx1100, sm_120a, …), native (whether that is the current device's ISA, i.e. whether the counted code is the code that ran), and registers (the VGPR count read from the AMD listing's metadata, to cross-check the runtime's attribute; nothing for SASS, where nvdisasm prints none — kernel_resources has the ptxas figure).\n\ntarget = \"gfx942\" / \"sm_90\" cross-compiles the same kernel — same function, argument types, always_inline and static workgroup size — for another architecture through GPUCompiler and counts THAT code, without the hardware: what the MI300X / H100 stream looks like, whether CDNA3 packed FP64 appears, how many waits the scheduler inserted. AMD targets take the gfx name optionally with HIP feature suffixes (\"gfx942:sramecc+:xnack-\"); GCN/CDNA (gfx9*) compile wave64, RDNA (gfx10+) wave32, like HIP. NVIDIA targets are sm_NN (sm_90, sm_100, sm_120a, …) as ptxas names them; a CUDA context must exist, not a device of that architecture. dump (an IO or a path) receives the disassembly.\n\nir = true (default) adds the ir field: kernel_ir_mix of the same job — the typed LLVM-IR operation counts BEFORE the backend, next to the machine-code counts (an IR fp64_fma of 0 against 219 DFMA per slot is the backend's FMA contraction).\n\nNothing is launched; the compile costs a few seconds. Static counts count code, not execution: read hot_loop for the per-iteration floor of the kernel's main loop and total for the whole binary including its cold exception paths; see fp64_issue_floor for turning the former into time.\n\n\n\n\n\n","title":"GPUDiagnostics.kernel_instruction_mix"},{"category":"method","location":"#GPUDiagnostics.kernel_ir_mix-Tuple{KernelAbstractions.Backend, CompiledKernel}","page":"Home","text":"kernel_ir_mix(backend, ck::CompiledKernel; target = nothing) -> NamedTuple\n\nTyped operation count of the OPTIMIZED LLVM IR of a compiled kernel's job (the same job kernel_instruction_mix disassembles; target as there), walked with LLVM.jl by opcode and operand type — the arithmetic before the backend's FMA contraction and div/sqrt expansion. Returns counts (a NamedTuple over IR_CLASSES, all functions of the module), fp64 (its FP64 sum), total, functions (Dict name → counts, for a module that still has out-of-line callees) and target. Whole-module totals: IR loops are not attributed (the machine-code hot loop is the per-slot figure; the IR is for the fusion/expansion ratio).\n\n\n\n\n\n","title":"GPUDiagnostics.kernel_ir_mix"},{"category":"method","location":"#GPUDiagnostics.kernel_resources-Tuple{KernelAbstractions.Backend, CompiledKernel}","page":"Home","text":"kernel_resources(backend, ck::CompiledKernel; block_size = something(ck.workgroup_size, 256))\nkernel_resources(backend, pattern; kwargs...) -> Vector\n\nCompile-time resource report of a compiled kernel (see compiled_kernels), and the theoretical occupancy the vendor runtime computes from it for a launch of block_size threads per block (default: the kernel's static KernelAbstractions workgroup size, else 256). Returns a NamedTuple:\n\nname, signature, block_size — identification and the block size the occupancy is for;\nregisters — architectural registers per thread (NVIDIA registers; AMD VGPRs);\nlocal_mem_bytes — per-thread stack/spill memory (NVIDIA local memory; AMD scratch, the private segment). Non-zero means register spills or a stack frame: every access is a global-memory round trip;\nshared_mem_bytes — static shared memory (LDS) per block the kernel descriptor RESERVES, whether or not the source declares any (LLVM's AMDGPU backend promotes private arrays it cannot keep in registers to LDS, sized for the kernel's maximum block size);\nconst_mem_bytes, max_threads_per_block — as reported by the runtime (-1 = not reported);\nactive_blocks_per_sm, active_warps_per_sm, max_warps_per_sm, warp_size, occupancy — the vendor occupancy calculator's resident blocks per SM (CU / WGP on AMD) at block_size, converted to warps (waves) and divided by the device's resident-warp capacity: occupancy ∈ (0, 1] is the theoretical occupancy the launch can reach. It accounts for registers, shared memory and block-size granularity together;\nmax_threads_per_sm, shared_mem_per_sm — device capacities, for attributing the limiter (shared_mem_per_sm ÷ shared_mem_bytes blocks is the shared-memory bound);\nisa — a Dict{String, Any} of extra figures read from the compiled code where the vendor exposes them: on AMD, sgpr_count, vgpr_count, agpr_count (CDNA), the sgpr_spill_count/vgpr_spill_count, scratch_bytes, lds_bytes, max_flat_workgroup_size, wavefront_size, code_bytes and the compiler's own occupancy_waves_per_simd estimate (register budget only — compare with occupancy to see whether LDS or the block size is the binding constraint); on NVIDIA the ptx_version and binary_version the kernel was built for, plus the ptxas --verbose figures of a regenerated compile with the kernel's own options: ptxas_registers, stack_frame_bytes (the call-ABI / private-array frame), spill_store_bytes, spill_load_bytes (true register spills — the driver's local-memory figure lumps both together), cumulative_stack_bytes, and ptxas_functions, the device functions assembled out-of-line (a non-empty list beyond the runtime's exception helpers means Julia did not inline them; compare CUDABackend(always_inline = true)). ptxas_matches_attributes says whether that regenerated compile reproduced the runtime's register count.\n\nThe pattern form maps compiled_kernels(backend; pattern) through the report (possibly empty). Nothing is launched; the AMD ISA dump and the NVIDIA PTX regeneration re-run the compiler for a few seconds.\n\n\n\n\n\n","title":"GPUDiagnostics.kernel_resources"},{"category":"method","location":"#GPUDiagnostics.launch_lane-Tuple{Nothing, Any}","page":"Home","text":"launch_lane(timer, backend) -> Int\n\nDevice id the loop's launches are filed under; resolve once per loop, not per launch.\n\n\n\n\n\n","title":"GPUDiagnostics.launch_lane"},{"category":"method","location":"#GPUDiagnostics.launch_tick-Tuple{Nothing, Any}","page":"Home","text":"launch_tick(timer, backend) -> event | nothing\n\nRecord the START event of a launch on the current task's stream (nothing timer ⇒ no-op).\n\n\n\n\n\n","title":"GPUDiagnostics.launch_tick"},{"category":"method","location":"#GPUDiagnostics.launch_times-Tuple{LaunchTimer}","page":"Home","text":"launch_times(timer::LaunchTimer) -> Dict{Int, Vector{Float64}}\n\nPer-device kernel seconds, one entry per launch in launch order. Waits on each launch's stop event, so it is safe to call as soon as the loop has returned.\n\n\n\n\n\n","title":"GPUDiagnostics.launch_times"},{"category":"method","location":"#GPUDiagnostics.launch_tock!-Tuple{Nothing, Any, Any, Any}","page":"Home","text":"launch_tock!(timer, lane, backend, e0)\n\nRecord the STOP event of a launch and file the pair (e0, stop) under device lane (from launch_lane). Call on the launching task, right after the launch (nothing ⇒ no-op).\n\n\n\n\n\n","title":"GPUDiagnostics.launch_tock!"},{"category":"method","location":"#GPUDiagnostics.measure_peak_fp64_flops-Tuple{KernelAbstractions.Backend}","page":"Home","text":"measure_peak_fp64_flops(backend; n_threads = 2^20, workgroup = 256, trials = 5,\n                        target_seconds = 0.2) -> Float64\n\nAttainable vector FP64 peak of backend in FLOP/s, measured with a dependent-FMA-chain kernel (8 independent chains per thread over n_threads threads). The chain length is calibrated so one launch lasts about target_seconds, then the best of trials launches is returned (best-of, like LinearAlgebra.peakflops, discards clock-ramp and scheduling noise). FLOP = threads × chains × iterations × 2 (one FMA = 2 FLOP). Never routed to matrix/tensor units: this is the ceiling for scalar FP64 code. Costs about (trials + 2) × target_seconds of device time.\n\n\n\n\n\n","title":"GPUDiagnostics.measure_peak_fp64_flops"},{"category":"method","location":"#GPUDiagnostics.rocprof_available-Tuple{}","page":"Home","text":"rocprof_available() -> Bool\n\nWhether rocprofv3 is on the PATH (ROCm 6.2+; the counter path of rocprof_command).\n\n\n\n\n\n","title":"GPUDiagnostics.rocprof_available"},{"category":"method","location":"#GPUDiagnostics.rocprof_command-Tuple{Cmd}","page":"Home","text":"rocprof_command(cmd::Cmd; counters, dir, name, timeout_s = 600, kernel_trace = true,\n                kill_after_s = 20, rocprofv3 = \"rocprofv3\") -> Cmd\n\nWrap cmd (environment and working directory preserved) in a rocprofv3 counter collection:\n\ntimeout -k <kill_after_s> <timeout_s> rocprofv3 [--kernel-trace] --pmc <counters…>\n    --output-format csv -d <dir> -o <name> -- <cmd>\n\ncounters is a name from ROCPROF_COUNTER_SETS or a vector of counter names. The outputs land in dir as <name>_counter_collection.csv / _kernel_trace.csv / _agent_info.csv for rocprof_counters(dir; name). rocprofv3 multiplies each dispatch's time by roughly its counter-read overhead only (kernels run at speed), but a counter request the hardware cannot serve aborts rocprofv3 (signal 6) and leaves the profiled child hung — timeout signals the whole process group when it expires and SIGKILLs it kill_after_s later, so the child dies too. run the result, or pipeline it into a log.\n\n\n\n\n\n","title":"GPUDiagnostics.rocprof_command"},{"category":"method","location":"#GPUDiagnostics.rocprof_counters-Tuple{AbstractString}","page":"Home","text":"rocprof_counters(dir; name = nothing, kernel = nothing, slots = nothing,\n                 n_cu = nothing, n_xcd = nothing, wave_size = nothing) -> RocprofCounters\n\nParse the rocprofv3 outputs <dir>/<name>_counter_collection.csv (+ _kernel_trace.csv and _agent_info.csv when present; name may be omitted when dir holds exactly one collection) into the per-dispatch counter table of the kernel whose name matches kernel (a Regex or substring; the match must resolve to ONE distinct kernel name). With kernel = nothing the run must contain exactly one user kernel (runtime-internal __amd_rocclr_* dispatches are ignored) and that kernel is selected; otherwise the error lists the kernel names to choose from. KernelAbstractions kernels are named gpu_<kernel name>(…) (AcceleratedKernels' foreachindex is gpu__forindices_global_(…)). slots is the number of inner-loop iterations (work-items × per-item iterations) of ONE dispatch, the caller's knowledge, for the per-slot metrics. n_cu, n_xcd and wave_size override the agent info — required when the run has no _agent_info.csv (the KNOWN device values; nothing is defaulted). n_xcd is the trap: every die (XCD) has its own GRBM block and the CSV reports GRBM_GUI_ACTIVE SUMMED over them (DIMENSION_XCC[0:7] on gfx942), so a 40 ms dispatch shows 5.7e8 \"cycles\" — 14 GHz — until divided by the 8 dies; the agent info's Num_Xcc is 8 on the MI300X and 1 on single-die parts, and every per-cycle rate below (unit busy, resident waves, clock) uses GRBM_GUI_ACTIVE / n_xcd. See rocprof_derived for what is computed from the table.\n\n\n\n\n\n","title":"GPUDiagnostics.rocprof_counters"},{"category":"method","location":"#GPUDiagnostics.rocprof_derived-Tuple{GPUDiagnostics.RocprofCounters}","page":"Home","text":"rocprof_derived(rc::RocprofCounters) -> Dict{String, Float64}\n\nDerived metrics of the kernel, each computed PER DISPATCH and reduced by the median — only those whose counters the run collected (a set from ROCPROF_COUNTER_SETS yields the metrics its docstring lists). With cycles = GRBM_GUI_ACTIVE / n_xcd (the dispatch's clock cycles on one die; the CSV sums the counter over the dies), n_cu / n_se / wave_size / max_waves_per_cu from rc.device and slots from the caller:\n\nclock_GHz = cycles / duration — the MEAN ACTIVE clock over the dispatch: GRBM_GUI_ACTIVE counts cycles while a die's GUI is active, so this equals the engine clock only while every die is busy for the whole dispatch and is a lower bound otherwise (idle tails count no cycles; it is meaningless for sub-ms dispatches, whose counter window exceeds the kernel). It is also the n_xcd sanity check (14 GHz means the die sum was not divided). Validated against the amdgpu sysfs engine clock (freq1_input sampled at 65 Hz through the same launches): 1.70 vs 1.70 GHz and 1.25 vs 1.26 GHz median. The clock is power-managed on the MI300X (1.7 GHz at 7 waves/CU, 1.25 GHz at 14 waves/CU on the same kernel — at the 750 W board cap), so compare two runs of the SAME work in CYCLES (GRBM_GUI_ACTIVE / n_xcd, SQ_BUSY_CYCLES), not in seconds: fewer cycles = more work per cycle; wall time falling less than the cycles = the clock dropped.\ninsts_per_slot_<class> = SQ_INSTS_<CLASS> × wave_size / slots for every SQ_INSTS_* counter (vmem_rd, valu, valu_fma_f64, …); fp64_flop_per_slot = (2·FMA + ADD + MUL + TRANS) × wave_size / slots when the four FP64 classes are present.\n<unit>_busy = <UNIT>_<UNIT>_BUSY_sum / (cycles × n_cu) for every *_BUSY_sum counter (ta_busy, td_busy, …); tcp_pending_stall = TCP_PENDING_STALL_CYCLES_sum / (cycles × n_cu).\nl1_miss = TCP_TCC_READ_REQ_sum / TCP_TOTAL_CACHE_ACCESSES_sum; l2_hit = TCC_HIT_sum / (TCC_HIT_sum + TCC_MISS_sum); l2_dram_read_frac = TCC_EA0_RDREQ_DRAM_sum / TCC_EA0_RDREQ_sum.\nwave_wait_frac = SQ_WAIT_ANY / SQ_WAVE_CYCLES (waiting for anything), wave_wait_inst_frac = SQ_WAIT_INST_ANY / SQ_WAVE_CYCLES (waiting for an instruction to issue; the difference is the dependency / memory-latency wait), wave_active_inst_frac = SQ_ACTIVE_INST_ANY / SQ_WAVE_CYCLES, wave_active_valu_frac = SQ_ACTIVE_INST_VALU / SQ_WAVE_CYCLES (all quad-cycle counters, so unit-free); resident_waves = 4·SQ_WAVE_CYCLES / cycles, waves_per_cu = resident_waves / n_cu, occupancy = waves_per_cu / max_waves_per_cu (rocprofv3's OccupancyPercent / 100); wave_cycles = 4·SQ_WAVE_CYCLES / SQ_WAVES (mean wave lifetime in cycles); sq_busy = SQ_BUSY_CYCLES / (cycles × n_se).\n\nThrows an ArgumentError naming the missing device value when a metric needs n_xcd, n_cu or wave_size and neither the agent info nor the caller supplied it; per-slot metrics need slots and are skipped without it.\n\n\n\n\n\n","title":"GPUDiagnostics.rocprof_derived"},{"category":"method","location":"#GPUDiagnostics.rocprof_manifest_section-Tuple{AbstractString}","page":"Home","text":"rocprof_manifest_section(dir; name = nothing, kernel = nothing, slots = nothing,\n                         n_cu = nothing, n_xcd = nothing, wave_size = nothing, prefix = \"rocprof_\")\n    -> Dict{String, Any}\n\nrocprof_summary of rocprof_counters(dir; …) with every key prefixed (rocprof_td_busy, rocprof_SQ_WAVES_median, …) — ready to merge! into a flat run-manifest table (e.g. next to the gpu_telemetry_stats keys of the same run).\n\n\n\n\n\n","title":"GPUDiagnostics.rocprof_manifest_section"},{"category":"method","location":"#GPUDiagnostics.rocprof_median-Tuple{GPUDiagnostics.RocprofCounters, AbstractString}","page":"Home","text":"rocprof_median(rc, counter) -> Float64\n\nMedian of counter over the dispatches of a RocprofCounters.\n\n\n\n\n\n","title":"GPUDiagnostics.rocprof_median"},{"category":"method","location":"#GPUDiagnostics.rocprof_summary-Tuple{GPUDiagnostics.RocprofCounters}","page":"Home","text":"rocprof_summary(rc::RocprofCounters) -> Dict{String, Any}\n\nFlat, manifest-ready reduction of a RocprofCounters: kernel (name up to its signature), dispatches, dispatch_median_s / _min_s / _max_s / _total_s, the kernel's grid_size, workgroup_size, vgpr_count, agpr_count, sgpr_count, lds_bytes, scratch_bytes, the device's device, n_cu, n_xcd, n_se, wave_size, max_waves_per_cu, slots (when given), counters (the names), <COUNTER>_median and <COUNTER>_rel_spread ((max − min) / |median| across dispatches) for every counter, and the rocprof_derived metrics.\n\n\n\n\n\n","title":"GPUDiagnostics.rocprof_summary"},{"category":"method","location":"#GPUDiagnostics.sampler_source-Tuple{AbstractString, Symbol}","page":"Home","text":"sampler_source(spec::AbstractString, counters::Symbol) -> SamplerSource\n\nBuild the source described by spec (\"<kind>:<payload>\"). Kinds shipped here: sysfs (amdgpu driver files: sysfs:<device>:<power_file>:<busy_file>:<membusy_file|->:<vram_file>) and synthetic (synthetic:<device>, deterministic fake values for tests); the CUDA extension adds nvml (nvml:GPU-<uuid>=<device>). counters is :auto (hardware counters when the device has them) or :none. Unknown kinds throw.\n\n\n\n\n\n","title":"GPUDiagnostics.sampler_source"},{"category":"method","location":"#GPUDiagnostics.telemetry_child_main-Tuple{AbstractVector{<:AbstractString}}","page":"Home","text":"telemetry_child_main(args) -> Int\n\nEntry point of the sampler child (a separate Julia process started by with_gpu_sampler). args: --dt=<s> --ppid=<pid> --stop=<stopfile> --counters=<auto|none> <spec>.... Opens one sampler_source per spec, prints a header comment naming the columns (# epoch_s  device  <metric…> — the union of the sources' first samples, base columns first), then one TSV row per device every dt seconds on stdout (nan for a missing metric), and returns when the stopfile appears or the parent dies. Never loads a vendor runtime beyond what the parent asked for (packages), so it starts in about a second.\n\n\n\n\n\n","title":"GPUDiagnostics.telemetry_child_main"},{"category":"method","location":"#GPUDiagnostics.thread_fill_occupancy-Tuple{KernelAbstractions.Backend, Integer}","page":"Home","text":"thread_fill_occupancy(backend, n_threads) -> Float64\n\nThread-fill occupancy: the fraction of the current device's total resident-thread capacity (gpu_sm_count × gpu_max_threads_per_sm) a launch of n_threads can fill (e.g. the pixel count of a pixel-parallel kernel). This is an UPPER BOUND on achieved occupancy — per-thread registers / shared memory cap it further; kernel_resources reports that compile-time bound.\n\n\n\n\n\n","title":"GPUDiagnostics.thread_fill_occupancy"},{"category":"method","location":"#GPUDiagnostics.with_gpu_sampler-Tuple{Any, Any, Real}","page":"Home","text":"with_gpu_sampler(f, backend, dt; devices = 1:1, tracefile = nothing, counters = :auto) -> (f(), telem)\n\nRun f() while a child process samples devices (1-based vendor ids) every dt seconds: power / compute / memory utilization / VRAM everywhere, plus hardware counters where the device has them (counters = :auto; NVIDIA GPM on Hopper and newer — achieved SM occupancy, FP64 / FP32 / tensor pipe and DRAM-bandwidth utilization, PCIe / NVLink traffic; :none skips them). f is first so the do-block form works. Without tracefile, samples go to a temp file that is deleted after parsing. telem is a GPUTelemetry column table; reduce it with gpu_telemetry_stats.\n\nIf no sampler child can be built for backend (no vendor extension, e.g. the CPU backend) or the child fails to start, a warning is logged and f runs without one — telemetry is never allowed to break the caller.\n\n\n\n\n\n","title":"GPUDiagnostics.with_gpu_sampler"}]
}
